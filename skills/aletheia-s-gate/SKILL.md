---
name: aletheia-s-gate
description: "METHOD: Gate 3 of Aletheia's six — check S/S′ stack coherence: every runtime, storage, workflow, harness or surface claim lands on the product that actually owns it (S0 Central, S1 Actuation, S2 AIKit, S3 Software Factory, S4 Workcell, S5 QL-MEF), uses a real installed command or Action rather than a hypothetical seam, and adds no second registry or store. Returns aligned, annotated, hold or redirect."
---

# Aletheia S gate (Gate 3)

## Contract metadata

- Semantic ref: `ql:skill:aletheia-s-gate` (`skill/ql/aletheia-s-gate`)
- Native checks: `ctrl actions --json` / `ctrl --json action list`; `aikit --help` and subcommand help; `factory capabilities --json`; `actuation capabilities --json`; `ql capabilities --json`; Workcell's own operation skill (`skill/workcell/workcell-operation`); `skill/ql/aletheia-stack-traverse` for the full walk.
- Source: `Body/S/S4/ta-onta/S4-5p-aletheia/S5'/skills/aletheia-s-gate/SKILL.md`, blob `f5de595e2e941a0e7309f44cc840c55e4f612f0e` (pinned and HEAD agree), with the stub `S5'/skills/gates/aletheia-s-gate.md` (HEAD blob `d718117b…`).
- Primary contact: Eros (`agent/anima-eros`). Moirai pattern: Lachesis — query the existing stack and registrations. Frame: CT3, CP 4.5. Human in loop: no.

## Checks

1. **Owner placement.** Each claim names its native owner: Central (source, NOW/Day, Positions, profiles, agent sets), Actuation (agency, occupancy, streams), AIKit (skills, knowledge, context, gateway, routines), Software Factory (workflows, runs, custody, Return), Workcell (material execution and machines), QL-MEF (kernel, MEF, Vāk, Technē language). S′ organs compose these; they do not re-implement them.
2. **Real surfaces.** Every command, Action and flag the artifact relies on exists in the installed binary's own disclosure. A documented-only command is reported as a claim that outran its implementation.
3. **No duplicates.** No second registry, store, runtime, identity or workflow engine; no parallel Wiki; no direct `wiki.json` edit.
4. **Return doors.** Writes go through the owner's door (NOW return, thought/learning, promotion, source return, PR), not through direct file writes into another owner's ground.

The source's older S-stack (terminal, Obsidian vault, Neo4j, a gateway on port 18794, Pi, Notion) is historical; judge current artifacts against the current products.

## Result

`aligned | annotated | hold | redirect`.

```text
GATE 3 (S): <artifact ref> -> <result>
Placement: … Surfaces checked: <commands> … Duplicates: none|<which> … Doors: …
```
