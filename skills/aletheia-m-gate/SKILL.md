---
name: aletheia-m-gate
description: "METHOD: Gate 2 of Aletheia's six — check an artifact's MEF and philosophical alignment: M-branch references and sub-branch notation, the MEF identities (M0 Anuttara … M5 Epii), lens use, and whether its metaphysical claims stay coherent with the authored ground and keep their standing. Returns aligned, annotated, hold or redirect. Use before an artifact that makes M-family or MEF claims is returned or promoted."
---

# Aletheia M gate (Gate 2)

## Contract metadata

- Semantic ref: `ql:skill:aletheia-m-gate` (`skill/ql/aletheia-m-gate`)
- Native checks: `ql mef lenses --json`; `ql epi-agent constitution --json` and `ql epi-agent faculty <#0..#5> --json`; `ql kernel ledger [coordinate]`; `skill/ql/ql-foundations` for L0/L1 meanings; retrieval through `skill/ql/gnosis-retrieve` first.
- Source: `Body/S/S4/ta-onta/S4-5p-aletheia/S5'/skills/aletheia-m-gate/SKILL.md`, blob `ee65169589bf0f3b4906eec8f666e5be74f696ba` (pinned and HEAD agree), with the stub `S5'/skills/gates/aletheia-m-gate.md` (HEAD blob `9a15dd60…`).
- Primary contact: Sophia (`agent/anima-sophia`). Moirai pattern: Atropos reflection, with retrieval of the relevant ground first. Frame: CT3, CP 4.5. Human in loop: no.

## Checks

1. **M notation.** M-branch references use the canonical identities and sub-branch notation: M0 Anuttara, M1 Paramaśiva, M2 Paraśakti, M3 Mahāmāyā, M4 Nara, M5 Epii; M′ for their instruments.
2. **Lens use.** Lens names and positions match `ql mef lenses`; a reading names the lens it reads through.
3. **Coherence.** Ontological claims do not contradict the authored ground they cite (DoV-grounded coordinate system, the locks under `docs/kernel-rebuild/` and `docs/integrations/epi-logos/`). Retrieve the ground before judging.
4. **Standing.** Each claim keeps its standing: authored position, design commitment, architecture contract, implementation fact, observed evidence or agent inference. A generated reading is not canon.

## Result

`aligned | annotated | hold | redirect` (see `aletheia-ql-gate` for the meanings). A claim that would change canon is `redirect` to `aletheia-collab-gate`.

```text
GATE 2 (M): <artifact ref> -> <result>
Checks: notation … lenses … coherence … standing …   Ground read: <refs>
```
