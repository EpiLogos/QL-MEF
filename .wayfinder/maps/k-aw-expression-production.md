# Wayfinder — K / AW Expression Production

**Owner:** QL-MEF #201 / #135  
**Ground:** `docs/integrations/epi-logos/K-AW-EXPRESSION-PRODUCTION-ALIGNMENT.md`  
**Prerequisites:** accepted K8–K10, #94/AW, Ta-Onta full-field lock, Vāk/Oikonomia lock, L5 Technē dual-reading lock.

**Current operative-form and testing continuation — 19 September 2026:** [Vāk Expression Workflow Wayfinder](../../docs/integrations/epi-logos/VAK-EXPRESSION-WORKFLOW-WAYFINDER.md) gives Q2/Q4 their concrete clean TypeScript authoring, first Factory-source stamp, real role/subagent, dynamic Expression-tool and curated-invocation work. It does not reopen K/AW production or replace Q0–Q4. Runtime agents receive lean Vāk/role guidance and actual tools, not this development map as a permanent prompt.

## Q0 — parent integration

The parent session owns shared interpretation, issue/Wayfinder propagation and final interface reconciliation. It does not redo all child work serially.

## Q1 — K/L5 continuity worker

Use **one development subagent**.

Recover the current accepted K8–K10 implementation and the L5 dual-reading relation. Produce a ledger across M0′–M5′ which names:

```text
M/Bimba domain relation
existing M′ instrument
current implementation/read model
Expression/Technē surface
native owner
shared subject/source refs
accepted evidence
real remaining adapter/implementation gap
```

Do not reinterpret M′ as generic view modes and do not reopen the engine architecture.

## Q2 — AW/Ta-Onta continuity worker

Use **one development subagent**.

Backcheck current S′ implementation against #94/AW and the source-pinned S4 architecture:

- six current organs;
- CPF/CT/CP/CF/CFP/CS composition;
- constitutional seven;
- Aletheia crystallisation/Return relation;
- six existing specialists and their guarded domains;
- Techne under Pleroma;
- current T/T′ / Recognition / Return path.

Report actual implementation gaps without minting new runtime roles.

## Q3 — Bimba Expression production worker

Use **one development subagent**.

Consume Q1/Q2 results and current Bimba registry/source to build the first source-bound production set:

```text
coordinate / branch
→ actual M′ instrument relation
→ inherited Profile / Scene / form grammar
→ editable Expression artifact
→ exact source/subject/relationship refs
```

Write artifacts into the dedicated QL/Bimba collection in Point-Cloud-Demo. Shared editor defects return to Point-Cloud #6 rather than being patched privately.

Prefer direct/disjoint artifact generation. No worktree by default for Profile/HTML/JSON/Scene output.

## Q4 — joined proof

Parent joins the three streams and proves representative cases spanning the existing M′ field and Ta-Onta sequence. The proof must preserve exact Bimba identity and native Actions from source through instrument to Expression/Technē presentation and Return.

### Vāk craft continuation within Q2/Q4

After the current cut settles, consume the focused VW0–VW6 packets in the linked Wayfinder:

```text
actual source/faculty/role coverage
   → [typed Vāk source + native Factory lowering]
   ∥ [live Expression search / inspect / act / observe]
   ∥ [clean canonical role/Skill delivery]
   → actual Factory expressive commission
   → curate, name, vary and invoke on later occasions
   → Z / T/T′ / independent Return in the same #65 campaign
```

TypeScript is the authored projection of the existing language and workflow contracts; it is not a second scheduler or Claude-only ontology. The source stamp precedes effects. Match actual participant IDs, loaded guidance, tools, task material and returned work; named roles in a file are not proof that subagents ran.

Keep whole-field coverage, source meaning and the existing constitutional/specialist field intact. The runtime needing a current Expression search/inspection/invocation tool is native implementation work, not another paragraph in a Skill. Preserve ordinary non-Factory performance while using Factory for this commissioned production and proving workload.

Runtime corpus materialisation uses O:I #366's actual source→Wiki/Knowledge→bounded whole→Expression path. Detached artifact generation remains useful authoring, not substituted proof of that live relation.

One reusable code worktree per coherent feature line, not per agent/session. Runtime sandboxes and disjoint artifact outputs are separate allocations. Do not touch the held mathematical M/M′ kernel, Mac QL installation or active frontend merely to prepare this line. Subsequent native-source/API repairs are explicitly owned and tested; publication of this map starts no workers and claims no live readiness.

## Return

Return one consolidated handoff:

```text
exact main/branch revisions
M′ continuity ledger
S′ continuity ledger
Bimba artifact collection status
source/Profile/Scene revisions
actual tests/evidence
cross-product debts by native owner
next representative production cut
```