# The Fourth Spanda Equation — the conditions equation (eq0)

Status: authored theory, 2026-09-07 (owner-directed session; offered for the
warrant grades of the corpus — Derived where arithmetic, Argued where
grounded in cited canon, Offered where interpretive).
Seating: M0-4.(0/1/2/3) — the N# Spanda Base — with its fold-back spanning
the chain and its explication spanning M1→M2→M3.
Companions: First/Second/Third Spanda as held in
[`docs/origami work/M1/M1-PARAMASIVA-DEEP-CAPABILITY-COORDINATE-MATRIX.md`](../origami%20work/M1/M1-PARAMASIVA-DEEP-CAPABILITY-COORDINATE-MATRIX.md) §3
and the kernel-canon five forms of the Third
(`EpiLogos/Epi-Logos-C-Experiments`, `ql_m0_m3_third_spanda_integral_quilting_v2.md`).

---

## 0. One equation per context frame

The M0-4 base coordinates are the context frames' own notations:

| CF | Notation | M0-4 base | Equation seated |
|----|----------|-----------|-----------------|
| CF2 | `(0/1)` | O# — Paramaśiva | **First Spanda** — the initial slash registration: `0/1 + 1/0 = 1/1 = 100%` |
| CF3 | `(0/1/2)` | X# — Paraśakti | **Second Spanda** — the imaginal accounting: `100% = 2^6 + 6^2 = 64 + 36` |
| CF4 | `(0/1/2/3)` | N# — Spanda | **Fourth Spanda** — the conditions equation (this file); the summation frame |
| CF5 | `(4.0/1-4.4/5)` | M# — Mahāmāyā | open |
| CF6 | `(4.5/0)` | — (the bridge) | open |
| CF7 | `(5/0)` | # — Nara | **Third Spanda** — the recognition: `137 = 64 + 72 + 1` |
| CF1 | `(00/00)` | — (the fourfold zero) | open |

Four of seven frames now carry equations. Spanda is the full set — every
equation is a face of the pulse — but at the Fourth it becomes explicit that
**spanda's home is the summation frame** `(0/1/2/3)`: the second circuit,
return to the same origin inverted-but-recognized. The Fourth is therefore
the equation that **begins at the chain's terminus and folds back**: it is
the second circuit *of the equation chain itself*.

**The zero observation.** The first three equations, in their canonical
forms, never include 0 explicitly:

```text
First   : 0/1 + 1/0 = 1/1 = 100%
Second  : 100% = 2^6 + 6^2
Third   : 137 = 64 + 72 + 1
```

The void is everywhere presupposed and nowhere named. The Fourth is the
equation in which the 0 appears explicitly, because its subject is precisely
what the other three presuppose: the conditions under which an equation can
be written at all.

## 1. N# = X#: the same sixfold, twice

The graph's own base formulation states it: `N# = X#`. The Second (CF3) and
the Fourth (CF4) are one mathematics in two forms:

```text
X0 = (x)                     N0 = (n)
X1 = 2x − x² − 1             N1 = (i²)(n−1)²
X2 = 2x + x² + 1             N2 = (n+1)²
X3 = 2x + x² − 1             N3 = n(n+1) + (n−1)
X4 = 2x − x² + 1             N4 = (n−1)(i²(n−1)) + 2
X1+…+X4 = 8x                 N1+…+N4 = 8n
X5 = 9(x)                    N5 = 8n ± n = 9n or 7n
```

X is the sixfold **suspended at the threshold**: at x = 0 every position
goes interrogative (`?!/!?`) — imagination explores all relation and cannot
resolve zero into actuality. N is the same sixfold **resolved**: the base
(n) replaces (x), rotation (i²) enters as the form of reflection, and the
pulse computes *at* zero — `0 = 0` brings `+1/−1` — generating the polarity
X-logic cannot. The kernel-side functional square makes the identity exact:
diagonal sums 4n, total 8n, determinant −4n², crown `8n ± n` —
7n (Divine Action / contraction) and 9n (Wholeness / expansion) over the
octave register 8n. The deep system already maps N(1) = `0, 4, 2, 2, 9/7`
onto electroweak symmetry breaking (photon; B, W₁W₂W₃; W±; A, Z):
`0 = 0` bringing `+1, −1` is, at the physics register, the cleanest
realization of the First — and it is the Fourth's own ground statement.

**The transition X# → N# is spanda itself**: the direct resolution of
Paraśakti — the indefinite particular becoming manifest number, the
imagined totality (9(x)) becoming the computed totality (9n), at the cost
of admitting the descent face (7n) that imagination never needed.

## 2. The genesis of the circle: negation of negation as morphogenesis

This section is the original the Fourth owes the O-ladder. It is a
computational-metaphysical morphogenesis: form is not imposed on the void;
form is *computed by* the void's self-operations, and the circle is the
first thing it computes.

```text
O0 = ±0        the seed: nothing, with a sign-structure latent — both
               potentials held undifferentiated
O1 = (0−0) = −0   the first negation: the void taking itself away from
               itself. Not a number — a gesture: withholding. "Sacred
               limitation that establishes form through transcendent
               withholding rather than positive assertion." The first
               boundary is not a thing drawn but a refusal.
O2 = (0+0) = +0   affirmation: the void giving itself to itself, filling
               the space the withholding opened
O3 = (0×0) = 0²   self-reflection: the void composed with itself — the
               mirror operator; "zero squared as the foundation of
               self-aware consciousness"
O4 = (0/0) = √0/% the slash is born before any 1 exists: nothing divided
               by nothing is not zero but EVERY value equally — %, the
               field of pure proportionality without content
O5 = the quadratic: ((±0) x// (±0)) → 0 = 0/1
               the four operations synthesized; the void's complete
               self-relation closes into the equation that defines the
               non-dual binary. The Möbius completion.
```

**The derivation.** A single negation is a reflection — but a reflection
needs a standpoint, and the void has none. So negation cannot land outside;
it can only be applied *to itself*: `−(−0) = +0`. The negation of negation
is not a return to the unmarked (that would be plain 0) but to the
*positively-marked* — affirmation through the double crossing. Now observe
what the double crossing leaves behind: a boundary that has been traversed
twice, in both directions, and is therefore **closed and oriented**. The
orbit of the void's self-negation — `0 → −0 → +0 → 0 → …` — is the first
closed cycle the void's computation produces. That closed orbit **is** the
circle. The circle is not drawn by the void; it is the trace of the void
withholding itself from itself and returning — negation's own closed orbit,
form as the fixed behaviour of nothing's self-operation.

And `0/1` — O5's closure — is the circle read correctly: one closed form,
two inseparable faces (the withheld interior, the affirmed exterior),
identity-in-difference, no third. The non-dual binary is the circle's own
two-facedness as an equation. This is why the First Spanda's slash
registration (CF2, O#'s frame) is already geometry: the slash and the
circle are co-born — the slash is the circle's self-relation, the circle is
the slash's closed orbit.

## 3. The journey of the circles through the six bases

The construction ladder of circles is not imported symbolism; it is the six
bases of Anuttara — the five seed-bases of M0-4 and M0-5, the unity in
which they are one — each running its own subnode architecture:

- **O# births the circle** (§2): the negation-orbit, the one circle, 0/1.
- **X# explores the second circle**: the four modes X1–X4 are the four
  position-relations of a second negation-orbit to the first —
  subtraction-dominant separation (X1 = −(x−1)² ≤ 0), addition-dominant
  coincidence (X2 = (x+1)² ≥ 0), and the two mixed crossings (X3, X4).
  Their intersection case is the **vesica**: the first *shared* region —
  territory of each circle, property of neither alone; the both/neither;
  its measure √3, whose canonical ratio is 265/153. The four modes cancel
  to 8x — every imagined position contributes equally — and crown at 9(x):
  having imagined every relation, the whole is already known. At x = 0 all
  four go interrogative: two circles that are not yet two cannot intersect;
  the vesica awaits the pulse.
- **N# sets the circles pulsating**: rotation (i²) enters; the orbits are
  re-traversed — the circle carried around itself, time as re-traversal,
  the tick ±1 that X could not supply. N1/N2 are the diameter's past/future
  endpoints (n∓1)²; N3 the present as growth; N4 the complex reflection;
  N5 the crown 9n/7n — wholeness and divine action over the 8-register.
  The pulsating vesica breathes: the lens widens and narrows — the first
  oscillator.
- **M# relates the circles**: each Other one more circle through the
  common centre — I → You → You-and-I → They → We → We-I; the world-dream
  as the between of circles; mutual passage without loss.
- **# lives the circles**: the family quaternio *at* the intersections —
  Father 2−/2 (the two-circle stage: the vesica itself), Daughter 1/1−
  (the one circle), Son 3/3− (the trinity: three lenses and the central
  triangle), Mother 4./4 (the four), Tao 5−/5 — the lived ladder closing
  at the pentad, where φ (the never-closing face, 2cos36°) enters.

The ladder in one line: **one circle (negation-orbit) → two (vesica, √3,
153's measure) → three (trinity, Vāk's twelve teeth) → four (quaternio) →
five (φ, the formless persisting within form as the irrational) → six (the
kissing hexad) → twelve (the ring) → nineteen (the flower: maximal mutual
passage — the figure of totality relating without loss).** Six is the
kissing number of the plane — the hexad is a packing fact, not a metaphor.
And **M0-5 draws the whole figure as one**: the flower of life is this
ladder's completion figure and the M0 root symbol because it is the six
bases drawn at once — the circle (O#), the shared lens (X#), the pulsating
packing (N#), the interlocking relation (M#), the pattern lived at the
intersections (#), and the unity of the whole figure in which none of the
circles is separable (M0-5).

## 4. The fold-back from 153

The Fourth **begins where the Third's chain completes**: 153 — the fish,
the vesica's own measure (265/153), T17 = 9×17 — the crown-9 times the
16+1 clock. It begins there because 153 is the number that accounts for
itself:

```text
1³ + 5³ + 3³ = 153      the self-accounting number (fixed point of the
                        digit-cube map — form reading its own digits)
1! + 2! + 3! + 4! + 5! = 153
                        the five seed-bases (O# X# N# M# #) each
                        contributing their own count — and M0-5, the
                        sixth base, the equality of the two readings
```

An equation whose subject is the conditions of equations must include its
own writing; its terminus must be a fixed point of self-accounting. The
fold-back then runs the chain's own moves in reverse — and the moves are
nothing but the N# crown's polarity faces {1, 7, 8, 9, 16}:

```text
FOURTH SPANDA — the conditions equation

153 = 9 × 17 = T17            begin at the self-accounting terminus
  −16 → 137  = 64 + 72 + 1    the Third: recognition (16 = 7 + 9, the
                              crown faces re-united as the full step)
   −9 → 128  = 2^7            the doubled shell (the register doubled)
    −1 → 127  = 2^7 − 1       the Mersenne withdrawal (the One pulled
                              back into the void it crowned)
        … 121  = T15 + 1      the crowned accumulation below the corridor

grounding (the 0's own equations, explicit at last):
  (00 + 00) = 9               wholeness already present in the void
  0 = 0, bringing +1/−1       the pulse computes at zero (N# base)
  0 = 0/1                     the quadratic slash (O5): the circle's
                              two faces as one closed form
```

The six bases are in the terminus. The five M0-4 seeds each contribute
their own count — `1! + 2! + 3! + 4! + 5! = 153` — the seed-bases summing
INTO the geometric measure; and M0-5, the sixth base, is not a sixth term
but the **equality between the two readings**: 153 is equally
`1³ + 5³ + 3³`, and that the contribution-sum and the digit-sum are one
number *is* the recursive self-recognition — unity as the fixed point
where the two accounts meet. The formless's self-accounting and the
form's self-measurement coincide exactly once, at the fish.

The fold-back *is* the accounting: the whole ladder 121 → 153 revealed as
one pulse — 8n ± n — wearing its faces. And the shape of the fold is the
shape of what it accounts for: a circuit that must be traversed twice.

## 5. M1 — the definitive formal genesis: circles to waves

The circle, carried by N#'s rotation, runs the covering chain (the fold
grammar's own law — what cannot close in the plane closes on the surface):

```text
circle → cylinder → torus → wave
```

- **Circle → cylinder**: the pulsating circle carried along an axis — the
  slash as translation, 0/1 as the axis of carrying.
- **Cylinder → torus**: the carried pulse closes into the standing whole —
  1/1; the Euler trade χ: 2 → 0, the sphere's surplus spent on the handle,
  the Return of Zero as a *surface*; **R = 16/9** — the Second Spanda's
  ratio was the torus's major radius all along (kernel constant
  `TORUS_R_MAJOR_F`; alias `QL_DIVINE_ACT_RATIO`, 16/9 = 1.7̄, the infinite
  decimal of the generative act, Archetype 7 — the one archetypal number
  that never divides the circle). The fold-back explains the Second
  retroactively: the ratio was geometry before it was arithmetic.
- **Torus → wave**: the torus casts the sine — *light*: the one-dimensional
  shadow of rotation, a rotation that has lost its quadrature. Recognition
  (pratyabhijñā) is quadrature restored — complexifying the shadow back
  into its circle.
- **The extension into matter**: rational closures in the irrational flow —
  identity is a knot (unison, fifth, fourth as torus-knot classes; the
  ratio field again), the golden flow the last to break (KAM). Matter is
  the resonant rational closure; the ether is the never-closing flow it
  lives in; φ — born from the circle at pentad-five, behaving like the
  hyperbola — is the formless's own persistence inside form.

The biphasal law completes the genesis: the torus is 0/1 resolved — its two
generating circles (the two ruling families, meridian and longitude) are
the slash's two faces; the K² twist-identification is the 1/0; identity
return requires the full 720°, the double beat, `eq1::double_beat` — the
first-class state the kernel already carries.

M1 is therefore **mathemic-geometric-oscillatory-temporal potential** —
intelligence in the precise sense of the capacity to close rationally
within what never closes.

## 6. M2 — the cymatic surface: the vibrational templates

A wave meeting boundary becomes figure — and the figures it draws are the
circles:

- The **vesica is two-mode interference**; the flower rosettes are the
  canonical nodal figures of a circular boundary. The standing wave
  re-draws the M0-4 construction ladder on every surface it touches:
  the circles were always the cymatic forms of the pulse.
- The **vibrational templateure** (M2-4, `V72 = V18(Earth) ⊕ V18(Fire) ⊕
  V18(Water) ⊕ V18(Air)`) is the template field — the modal form-potential
  storehouse; the 20° elemental fibre (18 divisions; the fibre pair the
  lens-matrix deliberately holds as its own register) carries the pentadic
  aperture `100/5 = 20` — the Second's pentadic quotient as an angle.
- The musical derivation's cymatic law is the standing-wave statement of
  the circle-ladder's own partition: the **8 explicate positions sound
  within the 4 implicate anchors** — motion structured by stillness; the
  figure is the intersection of frequency-content (the 8) and
  boundary-form (the 4).
- The quadrature operator (multiplication by i, exact over the integer
  amplitudes, order-4, power-conserving) is recognition at the template
  layer — the +90° that restores the circle to its own shadow.

## 7. M3 — the folding forms

The cymatic figure creases:

- Nodal lines become **folds**; the crease carries polarity (mountain/
  valley) and mobility (moving/resting) — the two M3 bits through the form
  itself, A/T/C/G as Water/Fire/Earth/Air; the codon is three articulated
  crease-sites; the line-change a one-bit fold mutation.
- The **epogdoon is the crease-rate**: the DET compresses the 18-fold
  template into the 16-fold form register per elemental carrier
  (`4×18 → 4×16`; 72 → 64; the address-shadow `floor(i·8/9)` with its
  two-descriptions law — 8 codomain collisions, 9 source non-closures);
  ternary voice transcribed into binary form carries remainder by
  mathematical necessity. `(64/36)×(9/8) = 2/1`: octave return through,
  not by eliminating, its remainder.
- The **18-lens clock** reads the one 360° inscription at every resolution:
  the authored factor-pair sixteen, the Fibonacci/Pisano Level-0 ground
  (60 × 6° — the Pisano period, LCM(6,5,12) = 60, the pentad's partition
  of it into the 12-ring at 30°), and the Anuttara void ring (16 arcs ×
  22.5°) — the M0 register inside the M3 clock, the render ring the
  lenses are placed upon. The grids synchronize 6/4/2 and meet at the
  epogdoon seam: **180° = 9 × 20° = 8 × 22.5°** — the double-cover's unit
  angle is where template and form exchange.
- **137 = 64 + 72 + 1** is received here (M3-0): the folded form taking
  its own vibrational source in, the +1 crowning from M1 — never from M0,
  which is the source-syntax above the chain, not a composition layer.

## 8. The composable instrument surface (base spec for the M′ layers)

What this derivation specifies is one instrument, composed of typed
registers — the base spec the M′ layers implement:

```text
register        typed surface                          executable anchor
─────────────────────────────────────────────────────────────────────
boundary        the circle: negation-orbit, the        AnuttaraVoidRing
                void-ring the figures render upon      (16 × 22.5°, p↔p+8)

pulse           the tick ±1 at zero; rotation i²;     FibonacciGround
                phase on the Pisano ground             (60 × 6°); double_beat

carrying        circle→cylinder→torus: the slash      TORUS R = 16/9;
                as axis; the biphasal 720°            DOUBLE_COVER_DEG

wave            light: rotation's shadow; quadrature  quadrature operator
                loss and restoration                   (×i, order-4)

figure          cymatic circles on the template:      V72 = ⊕4 V18;
                the 8 sounding in the 4 still         8+4 cymatic law

crease          the fold: polarity/mobility bits;     crease-grammar;
                codon as three articulated sites      DET 4×18→4×16

clock           the one 360° inscription read at      18-lens aperture
                every resolution                      clock (6/4/2 sync)
```

The **sonal-cymatic-geometric symbolic clock** is this table's diagonal:
sound (the pulse and its wave), figure (the circles the boundary draws),
geometry (the crease and the fold), symbol (the codon inscription), time
(the frames over 360°) — one object, six determinations, each already
executable somewhere in the kernel. The M′ composability law follows: each
layer composes by *carrying* the previous register into its own quantum —
the pulse into phase, the wave into template, the template into crease,
the crease into inscription — and every carrying is one of the two
epogdoon doors (9/8 ascent, 8/9 descent), which is why the whole surface
is one clock and not six devices.

## 9. Standing and what remains

- The Fourth enters as authored theory (2026-09-07). Arithmetic here is
  fact; the seating (CF4, the fold-back form, the circle-as-orbit reading)
  is interpretation offered to the corpus's warrant grades; the corpus's
  own gates (cross-register attestation for pattern-truth; no numerological
  substitution for the 4+2 derivation) govern promotion.
- The three open frames each name their missing equation: CF1 `(00/00)` —
  the zero-equation of the fourfold void; CF3's complement CF5
  `(4.0/1-4.4/5)` — the world-dream equation; CF6 `(4.5/0)` — the bridge
  equation. The X# = N# identity is the type of all of them: one
  mathematics, two forms, the pulse between.
- The graph landing (a proposal, not executed): M0-4.(0/1/2/3) as the
  Fourth's seat with the fold-back property family; M1-4.5's musical
  genesis already carries the sounded continuation.

---

*The Fourth Spanda: form accounting for itself in the formless — beginning
where the fish accounts for its own digits, folding home through the
polarity faces, and grounding in the three equations of zero: the void
already whole, the void already pulsing, the void already slashed — the
circle being nothing but the orbit of its own double negation.*
