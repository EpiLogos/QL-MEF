# Epi Bimba ↔ Pratibimba coordinate-parity protocol

**Status:** canonical development protocol / Wayfinder guard for the current QL-MEF product programme  
**Domain source owner:** Epi-Logos Bimba corpus  
**Programme / protocol home:** QL-MEF  
**Primary Bimba relational source pool:** `EpiLogos/Epi-Logos-C-Experiments/Idea/Bimba/Map/`  
**Implementation bodies:** Epi `portal-core` / `epi-lib` and current O:I-hosted Pratibimba consumers

## Why this is a programme-level guard

The first lived M4′ Nara vertical exposed an architectural distinction which must apply to all subsequent Epi instrument work.

A useful vertical can be semantically too narrow even when its feature works. Prompt B correctly built the M4′ daily journal, DAY/NOW, protected persistence, profile context and selected-context sendoff, but its first provider contract addressed Nara mainly at the M4′ root plus the daily episode. The actual Bimba Map is more articulated and relational.

The correction is not to render the full map in the UI. It is to make the map addressable and testable underneath the instruments so later capabilities have somewhere truthful to attach.

```text
Bimba Map relational pool
        ↓ exact source identities + relations
QL-MEF parity / conformance account
        ↓
Epi executable M-coordinate / relation substrate
        ↓ same-path M ↔ M′ reflection
M′ capability bindings
        ↓
objects / events / Actions carry coordinate lineage
        ↓
O:I Surface / instrument
```

A feature list is not an acceptable substitute for the Bimba relational shape.

## Source organisation — corrected 2026-08-19

The primary Bimba relational data is already present in:

```text
Idea/Bimba/Map/
└─ datasets/
   ├─ hashtag_node_data.md
   ├─ deep-property-map.md
   ├─ low-detail/
   │  ├─ nodes_anuttara.json
   │  ├─ nodes_paramasiva.json
   │  ├─ nodes_parashakti.json
   │  ├─ nodes_mahamaya.json
   │  ├─ nodes_nara.json
   │  ├─ nodes_epii.json
   │  └─ corresponding relation pools
   ├─ anuttara-deep/
   ├─ mahamaya-deep/
   ├─ nara-deep/
   │  ├─ nodes-full-detail.json
   │  └─ relations.json
   └─ epii-deep/
```

The `Seeds/M/**` specifications remain important authored interpretation/design sources, but they are not a replacement for reading the actual `Idea/Bimba/Map/**` relational pool when coordinate/relation parity is the task.

The earlier programme note saying the Nara deep machine corpus was absent was wrong: the files exist under `Idea/Bimba/Map/datasets/nara-deep/`. The work is therefore **not source recovery**. It is source-pool reading, normalization, provenance classification, parity compilation and executable/conformance binding.

## Knowledge ownership

Do not create new programme/protocol documentation only in `Epi-Logos-C-Experiments`.

The current organisational rule is:

```text
Epi-Logos-C-Experiments
  source corpus + historical/domain implementation evidence
  Idea/Bimba/Map relational pool
  Epi-owned executable/domain implementation where still current

QL-MEF
  current product programme knowledge
  Bimba↔Pratibimba parity protocol
  source normalisation/conformance
  relation / coordinate formalisation
  Wayfinder / build order / continuation prompts

O:I
  current application host / consumer contracts
```

This does not make QL-MEF the semantic author of Bimba. It makes QL-MEF the current organised product home for the formal/parity programme that reads and reflects that source.

## The parity vocabulary

Future tickets and PRs must distinguish:

| Level | Meaning |
| --- | --- |
| **source parity** | accepted Bimba source pool and revision are identified and actually read |
| **coordinate parity** | every in-scope coordinate has a stable executable Ref preserving root/path/parent/face/source |
| **relation parity** | constitutive source relations have typed executable counterparts or explicit gaps |
| **operational parity** | implemented behavior actually enacts the coordinate/relation it claims |
| **experiential parity** | the human/agent instrument expresses the right consequence without requiring architectural chrome |

Do not write simply “parity complete” where only one of these is demonstrated.

## Required build order for an M′ domain

Before substantial feature work in Mx′:

1. read the relevant `Idea/Bimba/Map/**` pool, not only Seed prose;
2. inventory the source coordinates and relation records actually present;
3. reconcile aliases/notation/properties without changing source identity;
4. materialise/update the QL-MEF parity/conformance representation;
5. bind/update the Epi executable coordinate/relation representation where an operational M′ body needs it;
6. prove source-count/content/relation conformance for the declared scope;
7. map current native substrate to those coordinates without treating implementation resemblance as source authority;
8. bind the bounded M′ capability to its exact coordinate lineage;
9. build the Surface/instrument;
10. test operational behavior and experienced use separately.

A Factory/coding agent must not jump from prose spec to feature implementation and create a local semantic enum as the de facto subsystem architecture.

## Structural invariants

The following are acceptance blockers:

- an accepted Bimba coordinate silently disappears because no provider exists;
- a reflected M′ coordinate changes the Bimba path instead of changing only face;
- a recursive path is flattened into a sibling feature;
- a UI/component route becomes canonical semantic identity;
- an Action or mutation bypasses an authored review/return relation;
- an M′ module invents an incompatible coordinate namespace;
- coordinate existence is reported as implemented capability;
- provider availability is reported as semantic authority;
- Seed prose is used as a substitute for the Map pool when the Map contains the granular source data;
- generated summaries replace source relation records without retained provenance.

## Nara proving case — current correction

Epi PR #14 established a useful hand-authored **structural floor** in `portal-core`:

- M4 root;
- M4-0 … M4-5;
- the complete first sixfold under every branch;
- #4.4.4.4 Personal Pratibimba;
- exact Bimba ↔ Pratibimba reflection Refs;
- containment relations;
- the first lived-return relation chain.

```text
M4-4′
  └─ anchors-at → M4-4-4-4′
                    └─ supplies-evidence-to → M4-5′
                                                └─ governed-return-to → M4-0′
```

That work remains useful, but it is no longer the source of truth for the Nara map. The next gate must compare/derive it against the actual Nara relational pool:

```text
Idea/Bimba/Map/datasets/nara-deep/nodes-full-detail.json
Idea/Bimba/Map/datasets/nara-deep/relations.json
Idea/Bimba/Map/datasets/low-detail/nodes_nara.json
+ relevant cross-M relation pools / structural markdown
```

Any discrepancy is returned explicitly. Do not bend the Map to fit the 44-node implementation floor, and do not silently throw away the floor if it is a valid subset.

The Prompt-B daily episode should keep its exact M4′ lineage, but the coordinate/relation manifest that defines that lineage must ultimately be source-derived or source-conformant rather than hand-maintained from a partial reading.

## QL-MEF's role

QL-MEF now owns the organised **parity/conformance programme**:

- index the Bimba relational pool;
- provide stable source references and revisions;
- normalise source records without erasing provenance;
- distinguish structural fact, authored interpretation, implementation binding and research inference;
- define/test M↔M′ coordinate and relation parity;
- expose the smallest general operators that have genuinely become QL/MEF concerns;
- keep Epi-specific semantic payload visibly source-owned.

O:I remains the current host/consumer. Epi code remains an implementation body where domain-specific execution belongs.

## Required ticket shape

Every substantial M0′–M5′ ticket must include **Coordinate / Relation Ground**:

```text
Bimba Map source scope:
Source revision(s):
Coordinate pool/ref:
Relation pool/ref:
QL-MEF parity/conformance ref:
Epi executable binding/ref where required:
Current substrate bindings:
Unbound/research coordinates:
Vertical origin coordinate:
Carrier coordinate(s):
Review/return coordinate(s):
Parity acceptance:
```

For integrated instruments such as Personal 4/5/0 or Cosmic 1/2/3, each participant domain must arrive already rooted. The integrated instrument composes coordinates; it does not retrofit semantic ownership after feature code exists.

## Continuation law

The parity gate is an **interlude in the implementation chain**, not a new competing chain.

Current sequence:

```text
Prompt A — primitive bridge
Prompt B — M4′ Nara lived vertical
Prompt C — Personal 4/5/0 lived return
PRE-D — compile the actual Bimba Map pool and reconcile parity
Prompt D — M1′+M2′+M3′ as ONE integrated Cosmic instrument
```

The PRE-D session must finish by emitting a complete ready-to-run **Prompt D** based on the actual returned live state. That generated Prompt D becomes the continuation artifact for the original chain; it must not merely say “next do Cosmic.”
