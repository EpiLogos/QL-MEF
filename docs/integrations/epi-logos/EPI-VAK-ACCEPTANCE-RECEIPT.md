# Epi Vāk acceptance receipt

**Status:** returned implementation/evidence receipt; **not** semantic authority  
**Owner programme:** QL-MEF #83 / PR #84  
**Returned implementation head before this receipt:** `4f42de910c857ff79503b671ce48d9d555dca370`  
**Final cross-owner acceptance run:** `33663063880`  
**Earlier cross-owner observed-path proving run:** `33662238612`

This file records the tested returned state of the Vāk programme. Meanings remain determined by the source map, governing architecture and native owner contracts; this receipt only says what was implemented and what was actually observed.

## Accepted composition

```text
AIKit ResolveExpression / ResolvePath
        ↓ exact syntax version + rendered expression + path identity
full Vāk reading/projection
        ↓
source-backed M0-3 / M0-4 / M0-5 field
        +
canonical QL shape kernel
        ↓
native product Action
        ↓
Invocation → Activity → Result / Return
        ↓ same-run evidence
OBSERVED VakPath
        ↓
DERIVED M5 Recognition + situated praxis reading
        ↓
Return to exact Vāk/source neighbourhood
```

The ownership boundary is explicit:

- **AIKit** owns the general operative AST, parser/renderer and `ResolvePath` identity. QL-MEF carries `VakGeneralExpressionEvidence` plus `VakExpressionReadingV1`; it does not implement a second parser or competing expression AST.
- **QL-MEF** owns the exact Vāk source language, Vāk readings, QL/Vāk relation field and Recognition/refraction semantics.
- **Each product** owns its native `ActionRef`, handler, authority gate and result lineage. Vāk profiles relate to those Actions; they do not replace their native meaning or execution.
- **Observed** means same-run execution evidence. Source/code inspection remains SOURCE / AUTHORED-ARCHITECTURE / IMPLEMENTATION as appropriate.
- **Recognition and praxis integration are DERIVED.** Observing an Action path does not promote Vāk source meaning, generated interpretation or a proposal into authored Ground.

## Exact external owner revisions

The final acceptance run checked out and exercised:

- AIKit operative syntax owner: `EpiLogos/ai-kit@4e35f499c50b987551ab124b4432757973e823ae`
- Factory Action owner: `EpiLogos/agent-system-design@71287c179e7686cb37cb25267958f0c390a1ecb7`
- Central Action owner: `EpiLogos/Central@77625f40081a3e38503d732f44c607a94888d9d6`

The AIKit proof constructed the same general expressions through the accepted parser/AST and verified their renderer and path identities:

```text
@2 candidate / @5 request-evidence
→ resolve-path:87fc6f42f239f59b

@4 Central/Work x @3 work.list
→ resolve-path:a8d70c5d3fef7c90
```

The Action proving specimens were the real native owner tests:

- Factory `request-evidence` — canonical `ActionRef action:01ARZ3NDEKTSV4RRFFQ69G5FAP`, authoritative executor, revisioned receipt and persisted HumanRequest return.
- Central `work.list` — native `ActionRegistry`, `WorkDiscovery` Port, structured `ActionResult::Success` and selected-connector diagnostics.

Only after both native executions succeeded did run `33663063880` expose its own run URL to the Vāk observed-path test.

## #83 acceptance return

1. **Exact 109-node source parity — IMPLEMENTED / VERIFIED.** Robust source-row parsing, source identity/provenance and formal-property coverage are executable and source-parity CI is green.
2. **M0-5 Śiva / Śakti — IMPLEMENTED / VERIFIED.** Six operators, six horizons, exact source bindings, 6×6 operative field and six same-position `/` generation sites are carried over the canonical QL shape kernel.
3. **M0-4 holographic relation field — IMPLEMENTED / VERIFIED.** Typed `## → O# → X# → N# → M# → # → R#`, contextual fields, Principle-9/R0–R5 paths and traversable neighbourhoods are executable.
4. **M0-3 self↔other language — IMPLEMENTED / VERIFIED.** Typed speech stances/forms and their source-grounded relations are executable.
5. **Will / Knowledge / Action in actual praxis — RETURNED AS SOURCE + OBSERVED + DERIVED.** The meanings remain source-backed; real Method/Action/Activity evidence is OBSERVED; their situated integration is `VakPraxisInstantiationV1` with DERIVED standing.
6. **Broad Vāk↔O:I primitive relation matrix — IMPLEMENTED.** Required O:I primitives are related through provenance-bearing readings without identity collapse or ownership theft.
7. **Representative native Actions across products — IMPLEMENTED / OBSERVED.** Factory and Central profiles pin native owner revision, handler/result lineage and Vāk affordances; both were executed in the final acceptance run.
8. **Method → Invocation → Activity → Return VakPath — IMPLEMENTED / OBSERVED.** `VakPathV1` cannot be reconstructed as OBSERVED without same-run evidence, matching owner revision and Invocation + Activity + Result/Return.
9. **Return from ordinary O:I actuality into Bimba/Vāk neighbourhoods — IMPLEMENTED.** Recognition retains exact Vāk refs and can reopen their source neighbourhoods; the native refs themselves remain native-owned.
10. **M5 Recognition / return — IMPLEMENTED / VERIFIED.** Recognition consumes an OBSERVED path, returns DERIVED field/praxis integration and leaves proposals distinct and non-promoted.

## Verification

Final four-owner acceptance run `33663063880` completed successfully with all of these stages green:

1. apply final semantic-boundary hardening;
2. prove exact AIKit-owned AST/render/path identities;
3. execute exact Factory and Central Action specimens;
4. `cargo fmt`, workspace check, clippy `-D warnings`, full QL-MEF workspace tests;
5. admit the same run as evidence into observed Vāk paths and M5 praxis Recognition;
6. commit the returned product state and remove temporary migration scaffolding.

On the human-triggered pre-return head `4c892e375d88de531020d4ceab32c9b8c16159e5`, the ordinary QL-MEF lanes were also green: Rust, Rust Verify, source parity, Bimba Map, Q6 pairing, Q6 MEF rotation, Q6 Context Frame, independent-Wiki conformance, Epi C R1/R2 and pre-local release.

## Return

The accepted relation is therefore no longer “a Vāk formal vocabulary beside O:I execution.” The executable field is:

```text
one general O:I expression
        /
one exact Vāk reading of that expression
        /
one native Action actuality
        ↓
one attributable path through Method / Invocation / Activity / Return
        ↓
one derived Recognition which can return to its exact source ground
```

This receipt does not close broader QL/MEF research, richer harmonic work, or O:I #97 whole-suite convergence. It records that the #83 Vāk runtime and its current O:I praxis acceptance have returned as tested product state.
