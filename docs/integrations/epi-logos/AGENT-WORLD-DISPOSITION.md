# Agent-world source disposition — AW0

This is the complete-source projection for QL-MEF#94, registered under the existing capability-matrix family in `.oi/product.json`. The [carrier](agent-world-disposition.json) binds the independently held inventories to named native mechanics and unresolved implementation obligations. [Exact source blobs](agent-world-source-blobs.tsv) qualify all reads. It does not replace any source matrix, the M ledger, a runtime registry or a native owner.

The governing meaning remains [the full Ta-Onta field](TA-ONTA-FULL-FIELD-LOCK.md) and [original musical Vāk/Oikonomia and Return](../../kernel-rebuild/VAK-OIKONOMIA-KNOWLEDGE-RETURN.md). Existing M capability IDs and complete deeper matrix bodies remain unchanged. K8.0 owns structural vocabulary, C parity and generated registry/census promotion; #134 owns full Nara. This projection supplies their source requirements without regenerating either lane.

## What is individually retained

The machine report includes all 36 SP00–SP55 historical source positions, six current organs and the entire root-contribution section; all 36 stable M capabilities and 36 M×S′ relations; all 149 deeper M1–M4 capabilities with every recursive tree, law, open question and drift record; all 109 Vāk entries and their seven original properties; all 36 operation/horizon interpretations; the six C′ offices, seven CFs, six CFP forms plus Z, six exact paired CS passages, twelve T/T′ meanings, six property offices and six Epii-on-X laws.

Original source bodies are independently section-addressed, not hidden behind a representative quotation. The original graph-schema crate is executed by a disposable read-only consumer. Its **164 node-property definitions, 30 relationship-property definitions, 25 relationship types, seven property families and six position semantics** remain attributable to that original producer. These are source-recovery records, not newly admitted C vocabulary. QV, numbered q_* fields, knowing and bake remain different named producers. The original 20-frame/40-directional statement has an explicit unresolved enumeration obligation: seven current CFs are not silently multiplied into a fabricated proof.

Each record retains its current or historical meaning, exact source locator/revision/blob, organ or coordinate, actual native API/source binding, effects/authority, Skill/Method participation, required Return/evidence and a named dependency. The complete expanded report is generated as an acceptance artifact rather than copied into a second enormous hand-maintained matrix.

## Readiness and observed evidence

The carrier's readiness reconciliation retains September's earlier matrix locks **as history** and identifies the accepted M1, M2, M3 and VakComposition engine receipts separately. Those mechanics are landed; their acceptance does not certify all Epi compositions. AIKit#302 supplies source-scoped Resolve and native Contemplate/Return; Factory#230 supplies native thought consumption. Their remaining joins stay on AIKit#267 and Factory#217.

Rows currently marked EPI-GAP mean that their **whole source-defined Epi composition** is not yet proved. They do not claim that every native component is absent. Native code/test-source inspection, executed source tables, accepted native behavior, current Epi composition and later owner-machine experience are different evidence scopes. No row gains ALREADY-REAL, READY-TO-COMPOSE or RESEARCH-ONLY without a separate evidence receipt and stated scope. Ordinary missing implementation is not relabelled research.

## Reproduce the full report

`python3 scripts/agent-world-disposition.py --self-test` checks the local registered carrier and all local source pins. It explicitly reports missing external owner observations; that is not a complete-source audit.

The `Agent-world source disposition` workflow checks out the carrier's exact native/original source revisions with no persisted credentials, runs `scripts/agent-world-property-source.py` against the original graph-schema without modifying it, then supplies those checked-out owners and the property receipt to `scripts/agent-world-disposition.py --complete --self-test --output evidence/full-field.json`. All source bodies, individual dispositions, checksums and the source-table execution receipt are retained in that artifact. A missing source, changed blob, omitted record, ambiguous binding or unqualified readiness promotion fails the audit rather than silently narrowing coverage.

This audit establishes source/disposition coverage. AW1–AW3 remain implementation and cross-owner acceptance work; it does not substitute a generated report for execution, source mutation, named praxis, useful consumption or human recognition.
