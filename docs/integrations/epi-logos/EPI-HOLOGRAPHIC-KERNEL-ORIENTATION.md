# Epi / QL-MEF holographic C-kernel orientation

**Status:** coordinate-integration account for the R1/R2 native C floor  
**Owner boundary:** generalized executable law in QL-MEF; Epi semantic/Bimba/data authority remains in `EpiLogos/Epi-Logos-C-Experiments`  
**Frozen Epi source:** `daa660cbc1b8c5da83828698665a753852cb0287`  
**R0 floor:** `d030823584d35eca4e27df60e6385d67a531026b`  
**R1/R2 line:** PR #59

This note is intentionally narrow. It records how the native C floor is to be read as a coordinate-bearing pratibimba without turning QL-MEF into the owner of Epi's Bimba, O:I's product field, AIKit's ProjectMap, Factory praxis, or application instruments.

## 1. Why the R1/R2 floor needs this layer

PR #59 has already established a useful and exhaustively checked low-level computational floor: six-position inversion, the 12-ring, 36 relation addressing, 64-state operations, 72-space resonance addressing, tritone-square identity, harmonic ratios, and epogdoon tick projection.

Those functions are deliberately small. Their mathematical usefulness does not require them to allocate or carry an ontology object at runtime.

For Epi, however, numerical equality is not sufficient evidence of full parity. A computational manifestation must also remain answerable to the structural subject it claims to embody. The correct layering is therefore:

```text
small C law
  + exact implementation identity
  + external coordinate/provenance account
  + numerical/behavioural evidence
  + explicit unresolved gaps
```

The machine-readable companion is `EPI-HOLOGRAPHIC-KERNEL-MANIFEST.json`.

## 2. C / C′

`C0..C5` is the archetypal category family:

```text
C0 Bimba
C1 Form
C2 Entity
C3 Process
C4 Type
C5 Pratibimba
```

This is epistemic/ontological qualification: what sort of thing, knowing, or manifestation is under account. It is not equivalent to a C language `struct`, enum, or compiler type.

`C′` is the operative/reflective **VĀK** family. Historical forms include `CPF`, `CT`, `CP`, `CF`, `CFP`, and `CS`. It concerns intentional articulation, composition, contextual organisation, invocation, orchestration, execution and return.

The primitive scalar C functions in PR #59 do **not** become VĀK merely because they are callable. The manifest therefore permits C′/VĀK to be absent or unresolved on a low-level law. VĀK becomes germane when those laws enter an intentionally composed act, Method, Context Frame reading, Run, instrument, or other operative circuit.

Where the repository fallback does not carry enough live Bimba evidence to assign a C category confidently, the manifest records the assignment as `supported-inference` or leaves it unresolved. It never fills a coordinate for visual symmetry.

## 3. S / S′ and the current product field

For this integration the six S roots are the current six O:I product centres:

```text
S0 Central
S1 Actuation
S2 AIKit
S3 Software Factory
S4 Workcell
S5 Quaternal Logic
```

The old Bimba S lattice remains developmental evidence but its former roots (CLI, vault, graph, gateway, ta-onta package, world return) are not retained as the present S roots.

`S′` is the lawful/reflexive technological refraction **across** those products. A capability may therefore appear through several product centres without becoming owned by the relation or by one implementation technology.

The first native C floor is materially owned by **S5 / Quaternal Logic**. That statement does not mean every consumer manifestation is S5, nor does it define a final S′ encoding. Cross-product refraction is recorded only where there is actual present evidence.

The human/agent `P/P′` product faces remain a separate axis. They are not renamed as S/S′.

## 4. Ta-Onta archetypes survive package dissolution

Historical Bimba development gave the former S4/ta-onta package a sixfold operational articulation:

```text
Khora     ground / bootstrap
Hen       form / content
Pleroma   capability / skill operation
Chronos   temporal pattern
Anima     contextual inhabitation
Aletheia  return / crystallisation
```

The package boundary may dissolve. The archetypes do not.

The manifest therefore has a named-archetype field capable of recording `Khora`, `Hen`, `Pleroma`, `Chronos`, `Anima`, `Aletheia`, `Sophia`, `VĀK`, and other Bimba-supported forms without equating an archetype with a package, provider, or product root. No primitive is assigned one merely because six names are available.

## 5. #5-2 / #5-3 / #5-4

The repository Bimba fallback at the frozen Epi revision confirms the structural branch names:

```text
#5-2  Siva-
#5-3  -Shakti
#5-4  Siva-Shakti
```

For the present architectural reading:

- `#5-2 Siva-` hosts the S/S′ technological, computational and infrastructural self-representation;
- `#5-3 -Shakti` hosts M′ embodied/instrumental/experiential manifestations;
- `#5-4 Siva-Shakti` hosts T/T′ thought, articulation and cognitive-emergence relations.

The old descriptive prose around these nodes is historical evidence, not authority to restore the old backend/frontend/agent-stack decomposition as ontology.

A native C function may therefore be an S5 computational manifestation under the #5-2 technological branch while a later O:I instrument is a distinct M′ pratibimba under #5-3. One does not exhaust the coordinate represented by the other.

## 6. Multiple pratibimba are lawful

A structural subject may manifest as mathematics, C computation, data, a generated LUT, an Agent faculty, a Wiki reading, a Factory Method, a Workcell execution, an O:I instrument, a visualisation, or another representation.

The manifest models manifestations as a list for this reason. It does not impose a one-Bimba-node/one-code-symbol law unless a source explicitly declares such cardinality.

## 7. Context Frames are referenced, not duplicated

Current QL-MEF formal Context Frame work owns the named/provenance-bearing projection over externally supplied sixfold compositions. Historical six-frame numbering and the current seven-frame grammar must not be silently identified.

The C/C′ integration therefore records Context Frame references only where source evidence warrants them. VĀK may invoke or execute through a Context Frame reading, but PR #59 does not create another Context Frame operator.

## 8. AIKit and Factory consumption boundary

AIKit's generic job is identity-preserving semantic ↔ executable reflection through ProjectMap/CodeIndex/Knowledge surfaces. An Epi provider may present a Bimba coordinate as a semantic endpoint and the exact C symbol as a code endpoint; AIKit does not become the owner of the coordinate taxonomy.

Factory's generic job is developmental fidelity. Where Structural Source Fidelity applies, Factory should consume the target-owned manifest as StructuralGround evidence, map the current implementation to it, and reject silent flattening, wrong parentage, stale reflection, or behavioural-only parity. Factory does not become the Bimba owner.

## 9. T / T′ continuation

The current seed tree preserves `T0..T5` and their conjugate directories, while the frozen repository fallback does not by itself supply enough current semantic relation detail to declare the full technological mapping.

The next T/T′ tranche should start from `#5-4 Siva-Shakti` and relate thought/articulation to human-authored ground, Agent Wiki knowledge, SemanticWiki readings, source descriptions, Skills/Methods, Runs, Claims/Evidence, temporal return and Recognition. The present manifest preserves T/T′ relation slots without inventing those assignments now.

## 10. Acceptance consequence for PR #59

The native primitive API should remain small and unchanged unless a computational type itself needs new information. For the current tranche it does not.

PR #59 is therefore to be read as:

```text
generalized primitive law layer
        ↓
external holographic identity/provenance manifest
        ↓
richer Epi coordinate kernel and product manifestations later
```

The manifest validator makes it an error for a public native primitive to exist without an explicit structural account. Unresolved semantic relations remain valid data in that account; silent orphaning does not.
