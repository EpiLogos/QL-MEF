# Epi / Pratibimba Prompt C — Personal 4/5/0 Returned Reality

**Status:** development-state Wayfinder addendum; not accepted-main state  
**Parent Wayfinder:** `EPI-LOGOS-DEVELOPMENT-WAYFINDER.md` on Prompt-B returned-reality PR #50  
**Programme:** #30  
**Pratibimba instruments:** #34  
**Personal source law:** #35  
**0/1 parent:** #36  
**Epii / return law:** #37

## 1. Exact live stack

Prompt C was developed on the actual Prompt-B branches rather than silently rebasing onto repository `main`.

```text
Epi PR #8   Prompt A primitive bridge
   ↓
Epi PR #9   Prompt B protected Nara store / stable selection
   ↓
Epi PR #11  Prompt C Epii + Anuttara/Bimba + proposal provider

O:I PR #87  Prompt A Epi host
   ↓
O:I PR #88  Prompt B Nara lived vertical
   ↓
O:I PR #89  Prompt C Personal 4/5/0 experience / governed return
```

Prompt C's Central return integration consumes the real ProjectCentral NOW/DAY owner contract on **Central PR #75 / `agent/projectcentral-now-day`**. That producer is green but remains an open draft and is not represented here as Central `main` state.

The coordinated Prompt-C Epi producer revision pinned by O:I is:

```text
80f13d9e2e534ee87dd224bb5712446d50f7b963
```

## 2. The Personal experience is now one Nara-rooted relation

The default remains the Prompt-B Nara daily canvas. After the human selects a real passage and performs the existing governed sendoff, the same selection can explicitly summon:

```text
Explain
Review
Source
Bimba
Provenance
Proposal
History
```

The depth surface occupies the same primary Epi canvas and returns to Nara. The existing situated/root-agent region continues to show the same stable selection packet.

There is no:

- Epii dashboard beside Nara;
- Bimba dashboard beside Nara;
- duplicate conversation document;
- DOM automation for co-reference;
- renderer-owned cosmology or private identity model.

The stable object carried through the loop remains:

```text
episode ref
+ exact UTF-8 byte range
+ episode revision
+ selected text
+ DAY/NOW
+ QL address
+ Epi coordinate ref
+ harmonic/profile ref
+ disclosure scope
```

## 3. M5′ Epii — first implemented role

Epii is now materialised at the semantic/product level as the canonical Epi #5 identity:

```text
epi:agent:epii
#-5
```

Its Prompt-C function is specifically:

- explain the current Nara/lived Epi state;
- review the exact selected passage;
- keep authored / observed / inferred / derived / formal / research standing explicit;
- explain the difference between a generated reading and an adopted truth;
- expose source/Bimba/QL/provenance summons;
- form an unreviewed Personal proposal;
- preserve the four different return authorities.

The Epi provider explicitly records the carrier/body boundary:

```text
canonical Epi Agent identity
    ↓
Actuation Agent / Agency + WorldBinding
    ↓
AIKit Profile / SkillSet / Context / model / harness / SessionSpace
    ↓
Workcell only where a material body is required
```

No `EpiiRuntime`, `EpiSession`, model selector, harness owner, coding-agent manager or terminal/process runtime was added to Epi or O:I.

This tranche does **not** pretend a live model-backed Epii conversation exists where the current native Actuation/AIKit adapter has not yet been exercised. The implemented Epii operation is an Epi-owned pedagogical/review read-model over the protected selection, governed as a canonical Action. Live model/harness materialisation is an intentional deeper M5′ step.

## 4. M0′ Anuttara / Bimba — focused ground orientation

The same protected selection can be passed through the governed Anuttara grounding Action.

The provider keeps the relation inspectable:

```text
Nara selection
    ↓ via Epii review ref where present
Anuttara grounding
    ↓
current Bimba semantic locus
+ source anchors
+ current EpiAddress
+ QL address / provider revision
+ explicit reason the ground was selected
```

The reason is structural rather than keyword-based: the selection already carries its canonical Epi coordinate and formal orientation. M0′ follows that stable relation instead of copying the selected prose into a new search/conversation identity.

Bimba identity remains explicitly distinct from its transport/provider:

```text
Bimba semantic Ref
!= Neo4j
!= MCP
!= provider binding
!= QL-MEF Meta-Knowledge Graph
```

The first Personal loop does not invoke a graph provider. Provider status is reported as such; provider identity is never allowed to become Bimba semantic identity.

QL/refraction is formal/advisory in this operation. No M0′ grounding call promotes Epi or QL canon.

## 5. Human personal return is owner-gated

Prompt C now has a concrete proposal and Central return boundary without collapsing proposal into adoption.

Epi proposal state is born as:

```text
sourceClass = proposal
adoptionState = unreviewed
sourceMutationPerformed = false
```

Allowed resolutions remain:

```text
retain-as-derived
reject
human-review
```

When Central NOW is configured, O:I returns **bounded refs and safe status text only** through `projectcentral.now.return`. It does not place the protected selected text or proposal body into Central process arguments.

Rejection is a separate `projectcentral.now.update` path and is a valid complete outcome.

Durable personal source return is deliberately separate again. The source must already be a human-owned `ProjectCentral/now/user/**` source and Central independently requires:

```text
projectcentral.now.promote
acceptance = human-accepted
```

Thus:

```text
Epi-generated proposal
   != human-authored Central source
   != accepted mutation
```

Epii can help formulate the difference; it cannot acquire human authorship authority.

## 6. The four return classes survived implementation

Prompt C preserves the #37 law in code and tests:

1. **formal-model return** — domain/formal recurrence;
2. **operative software return** — native Project/Factory/repository recognition;
3. **Epi theory/canon return** — explicit Epi/QL source amendment/recognition;
4. **human personal return** — explicit human-owned Central/Nara recognition.

There is no generic `return()` mutator that crosses these authorities.

## 7. Acceptance evidence

### Epi producer

Prompt-C Epi PR #11 at `80f13d9e2e534ee87dd224bb5712446d50f7b963` is green.

Its real `pratibimba-bridge` tests prove:

- protected Nara episode write and exact selected range;
- Epii review over that exact subject;
- Anuttara/Bimba grounding over the exact same `selectionRef`;
- exact episode/profile/coordinate retention;
- proposal creation with no source mutation;
- restart returns the same review/selection/episode identity;
- ambient private prefix/suffix does not leak into the returned review;
- stale/forged selection fails closed;
- no parallel Epii runtime or generic return mutator;
- Bimba provider identity does not replace Bimba semantic identity.

### O:I consumer

On the Prompt-C O:I branch, the real-provider acceptance job checks out the exact Epi producer above, compiles/tests the real C/Rust-backed bridge, and then proves the O:I host against that binary.

The completed Prompt-C acceptance steps are green for:

- Epi producer build/tests;
- Prompt-B protected Nara provider tests;
- O:I Personal return tests;
- Phase-4 execution/authority containment;
- Nara-rooted Personal presentation contract;
- full O:I desktop presentation;
- native Rust shell;
- OI Verify;
- full-suite clean-environment preflight.

During the tranche, two failures were treated as returned reality rather than ignored:

1. the cross-repository workflow called a nonexistent `npm test` script; it was corrected to run the repository's actual `node --test .../nara-presentation.test.mjs` contract;
2. the Tauri Personal dispatcher bound an impossible unreachable tuple, causing native-shell type inference failure; the command was simplified to return directly from each `match` arm.

Both failures disappeared under the full composed checks after correction.

### Central owner contract

Central PR #75 is separately green and proves:

- `projectcentral.now.inspect`;
- `projectcentral.now.return`;
- `projectcentral.now.update`;
- `projectcentral.now.promote`;
- explicit `human-accepted` promotion for human temporal material;
- attributed Agent-return separation;
- NOW/DAY remains a temporal working field rather than canon, Run, Session, Focus or Wiki.

Prompt C consumes that contract; it does not relocate its authority into O:I.

## 8. What remains intentionally deferred

### M5′ deeper work

- live model-backed Epii conversation through native Actuation/AIKit materialisation;
- deeper Epii pedagogy and multi-turn recursive review;
- source/canon recognition workbench;
- Logos Atelier / self-articulation workspace;
- source diffs and broad Epi development tools;
- historical Theia/Electron/Tauri generic shell mechanics except where useful as migration evidence.

### M0′ deeper work

- full Bimba graph traversal/exploration;
- graph editing and relationship-authoring UX;
- provider-backed Neo4j/MCP traversal where appropriate;
- 3D/spatial graph views;
- evolution/timeline/source-diff inspector;
- full technical M0′ workbench.

These are deferred because the first Personal loop only needs reliable orientation to the relevant ground, not permanent exposure of the entire graph substrate.

## 9. Returned build order

The first Personal loop is now real enough to advance the parent without fragmenting the Cosmic side.

The next implementation handoff is exactly:

> **PROMPT D = M1′ + M2′ + M3′ as ONE integrated Cosmic instrument, fed by real shared computation and feeding the same Nara world.**

Do **not** hand off “build M1 next”.

The next proof should show that the Cosmic instrument consumes the already-real shared primitive/harmonic/temporal state and that Nara receives those same canonical handles without a second renderer-local cosmology.