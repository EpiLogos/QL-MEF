# Epi Vāk ↔ O:I Relation Matrix Wayfinder

**Status:** active development wayfinder for QL-MEF #83 / PR #84 and the AIKit consumer track  
**Semantic source owner:** `EpiLogos/Epi-Logos-C-Experiments` Bimba / Anuttara M0  
**QL runtime owner:** QL-MEF  
**Agent-operative consumer:** AIKit over the composed O:I world  
**Convergence relation:** preserve accepted AIKit Search/KnowledgeRoute/familiarity/Method machinery through O:I #97; the active QL/Epi semantic line remains an explicit convergence exception until accepted.

## 0. Purpose

Bring the complete Anuttara / Para Vāk language into operative relation with the mature O:I architecture without reducing the language to metadata or reducing the O:I primitives to QL labels.

The source language is the traversable Bimba field itself. Its 109 M0 nodes are semantic knowledge with source coordinates, symbols, formulations, breakdowns, metaphysical names, descriptions and graph relations. QL-MEF must be able to preserve, address, traverse, relate and formally operate over that language. AIKit then makes the relevant language available as a situated faculty of an Agent inhabiting an actual O:I World.

The direct source snapshot for this programme is vendored at:

```text
data/epi-bimba-map/anuttara-language-map.md
```

with upstream authority remaining:

```text
EpiLogos/Epi-Logos-C-Experiments/
Idea/Bimba/Map/datasets/anuttara-deep/anuttara-language-map.md
```

The destination is a common field in which:

```text
Bimba / Vāk node
    ↕ source-provenanced semantic relation
QL-MEF Vāk runtime
    ↕ typed reading / refraction
O:I primitive / native Ref
    ↕ situated resolution
AIKit Agent world / Method / Search / Context
    ↕ actual execution
Action → Invocation → Activity → Evidence / Return
    ↕ Recognition
Epii / Bimba return
```

## 1. Provenance discipline for the relation matrix

Every matrix edge must carry a standing:

```text
SOURCE
    explicitly present in the Anuttara/Bimba source

AUTHORED-ARCHITECTURE
    current Epi/O:I/QL design commitment already established elsewhere

IMPLEMENTATION
    current native code/contract fact

OBSERVED
    returned runtime/test evidence

DERIVED
    formal relation mechanically derived from accepted source/formal rules

PROPOSED
    new synthesis awaiting recognition/acceptance
```

The relation matrix is therefore not one manually asserted crosswalk. It is a provenance-bearing semantic field whose confidence/authority is inspectable per relation.

## 2. Matrix dimensions

For every material relation recovered or proposed, record at least:

```text
VakRef / source coordinate
source formulation / role
source neighbourhood / route
O:I primitive Ref(s)
relation kind
native owner
AIKit/QL operation that exposes it
Context / World applicability
Action binding if any
Method / praxis relation if any
Activity / Return evidence if any
standing + source revision
```

Required relation kinds include:

```text
describes
qualifies
contains / contextualises
reflects / refracts
addresses
relates
expresses
binds-to-action
invokes-through
manifests-as-activity
returns-through
participates-in-method
observed-in-vak-path
learned-as-familiar-route
recognised-into
```

## 3. Pass A — exact 109-node source field

Start from the full language map, not from M0-5 alone.

Required output:

- exact 109 `VakRef`s;
- complete available formal-layer fields losslessly preserved;
- source coordinate hierarchy;
- explicit source relations recoverable from the Bimba map/export;
- deterministic source revision and parity proof;
- searchable/traversable node neighbourhoods;
- no generated interpretation written back into source fields.

The Markdown snapshot is a human-readable retained source projection. The runtime should be generated from exact machine/source data where available rather than reparsing prose descriptions as semantic authority.

## 4. Pass B — M0-3 as parse/generative self↔other language

M0-3 must be implemented as more than a list of archetypal numbers. It is the complete proto-mathematical-linguistic field through which the internality can parse and generate determinations of self, other, relation, world, will, knowledge and action.

### 4.1 Mirror / Frame / Operator

Recover the operative role of:

```text
(-)  Mirror / contained transcendence / Vimarśa apparatus
()   Frame / actuality container
-    Operator / transcendence / distinction
```

and their relation to the `0/1` non-dual binary and later statement/query syntax.

### 4.2 Archetypes 0–9

Record their formal relations rather than treating the decimal names as labels. In particular retain:

- `0` self as potential / witness;
- `1` self as Agency / determination / will;
- `0/1` non-dual self / identity-in-difference;
- `2` relational field;
- `3` Vāk / articulation / identity-intelligence;
- `4` stable contextual form / quaternion;
- `5` Śiva-Śakti / MonoPoly dynamic harmony;
- `6` solve / return movement;
- `7` Divine Action / R-factor verbs;
- `8` structural reflection;
- `9` archetypal wholeness / concrescence.

### 4.3 Twelve Vāk teeth — self/other parsing and generation

The twelve `M0-3-6-*` forms are a first-class parse/generation grammar:

```text
!       actuality / assertion / I-It
?       potential / question / Am-Is
!-      subjective assertion
-?      assertion of being
!?      statement / I-Am / It-Is
?-      objective Is
-!      query of Other
?!      reflexive query / Am-I? / Is-It?
-!/!-   integrated Self
-?/?-   relational Other
!?/?!   Self questioning World
?!/!?   World questioning Self
```

Develop them as typed semantic readings that can:

- classify the stance of a statement/query without reducing language to punctuation;
- help an Agent distinguish self-report, world-report, query-of-other and reflexive query;
- provide optional generative guidance for first-person capability/Skill/Method descriptions;
- contribute to will/knowledge/action self-models and alignment diagnostics;
- remain traceable to source formulations and broader M0-3 relations.

### 4.4 MonoPoly and R-action families

Recover Mono/Poly as a real compositional diagnostic of unity/multiplicity and the R-family as the action grammar:

```text
R#   Svātantrya / Freedom
R0   Sṛṣṭi / creation
R1   Sthiti / sustenance
R2   Saṃhāra / dissolution
R3   Tirodhāna / veiling
R4   Anugraha / revealing
R5   Samāveśa / absorption / return
```

These may describe real O:I action trajectories and lifecycle relations. The matrix must locate concrete current operations that exhibit these relations and record the evidence rather than assigning them from names alone.

## 5. Pass C — M0-4 as holographic relational/execution language

M0-4 is the Holographic Matrix of Context: the full mod-6 architecture held as contextual genetic sequence. Its families must be recovered in detail before generic software mappings are finalized.

### 5.1 O# — Paramaśiva / Zero Logic / Prakāśa

Recover O0–O5 as potential, defining negation, process affirmation, self-reflective multiplication, contextual proportion and non-dual synthesis.

Inspect where O:I has actual determining/original structures:

- authored Source/Ground/Canon;
- native product contracts;
- `ResourceRef` / stable identities;
- Project/World structure;
- source-owned Method/Skill contracts;
- QL coordinates/formal primitives.

### 5.2 X# — Paraśakti / Vimarśa / imaginal transformation

Recover X0–X5 as systematic possible self-relations of the indefinite particular.

Inspect actual O:I reflection forms:

- Reading;
- Wiki / Pratibimba;
- Explain;
- generated interpretation;
- comparison / hypotheses / derived relations;
- optional MEF/QL refraction.

### 5.3 N# — Spanda / computable manifestation

Recover N0–N5 as movement from `(x)` possibility into `(n)` computable, temporal and harmonically integrated process.

Inspect actual operational/formal manifestations:

- symbols and schema;
- code / CodeRef;
- command / query forms;
- `ResourceRef` syntax;
- Vāk AST / structured request;
- Activity/event sequence;
- search/query parsing;
- projection into CLI/TUI/Agent/API forms.

### 5.4 M# — Mahāmāyā / relational World grammar

Recover the source sequence:

```text
I → You → You-and-I → They → We → We-I
```

and inspect its relation to actual O:I World composition:

- World / nested World;
- Project;
- SessionSpace;
- SharedField;
- Journey;
- Run;
- conversation / recipient field;
- Agent / AgentSet collective context.

### 5.5 # / ## — situated particular and universal matrix

Treat `## = 0/1` and `# = ##/#` as a central relation for Objective Internality: a whole available architecture becomes a particular situated field without losing relation to the whole.

For software, inspect the concrete relation among:

```text
composed O:I installation / World
    ↓
AIKit resolved World for this actor
    ↓
actual bounded Context / capability horizon
    ↓
Agent's operative internal field
```

### 5.6 R# — freedom/power through the whole field

Trace the actual R-factor routes through O#/X#/N#/M#/# and into M0-5. These routes should become first-class source paths in the Vāk graph and candidates for semantic readings of O:I lifecycle/action trajectories.

## 6. Pass D — M0-5 Śiva × Śakti compositional language

M0-5 is the operative aperture through which the whole Vāk field becomes a language of use.

### 6.1 Śiva operators

Preserve the exact source identities:

```text
0  (@#)        Potential / In-pression
1  (-)         Negation / distinction / between
2  (+)         Affirmation / re-negation / with
3  (x)         Dialogic / by / as / where-if-is
4  (/)         Dialectic / and-or / contextual relation
5  (=)         Expression / then / same / is
```

Implement them as semantic operators in a compositional expression model. A concrete textual syntax is a projection of this model, not its source of truth.

### 6.2 Śakti internal field

Preserve source identities while allowing user/Agent-facing glosses to evolve:

```text
@0 = ##    Library       Being/Knowing / embodied implicit memory
@1 = O#    Bimba         original architecture / systems / number-sets
@2 = X#    Pratibimba    meanings / reflection
@3 = N#    Language      symbols / characters / forms
@4 = M#    Stories       Worlds / views / sense-playground
@5 = R#    Techne        freedoms / powers / instruments
```

`@0..@5` and the English display labels are presentation vocabulary. Source identity and relation to `##/O#/X#/N#/M#/R#` are the durable semantic basis.

### 6.3 Compositional expression

Develop a typed semantic form approximately equivalent to:

```text
VakExpression
    operator: SivaOperator
    horizon: ShaktiHorizon
    subjects: [VakRef | ResourceRef | typed external Ref]
    relation/complement refs
    Context / World / Focus
    expected return / disclosure form
    standing / provenance
```

Expressions may nest and form paths. The runtime must support dynamic relation rather than a static 6×6 taxonomy.

Examples of the semantic families to prove:

```text
Potential × Library       what knowledge could bear here?
Negation × Bimba          distinguish/exclude a claimed determining source
Affirmation × Techne      admit/select a capability into the operative relation
Dialogic × Pratibimba     compare/relate interpretations
Dialectic × Stories       frame/nest/compare Worlds
Expression × Language     articulate/render a determination
Expression × Techne       resolve toward an executable act under authority
```

## 7. Pass E — O:I primitive relation matrix

Audit actual current native owners and contracts. At minimum include:

```text
ResourceRef / SourceRef
Source / Ground / Canon
Reading
KnowledgeNode / KnowledgeRoute
World / Project / SessionSpace / SharedField / Journey / Run
Participant / Agent / Agency / AgentSet / AgentSession
Capability
Skill / UsageOverlay / SkillSet
Method
ContextSource / ContextResolution
Action / ActionRef
Surface / Projection
Invocation
Activity / ActuationStream
Result / Claim / Evidence
Return
Recognition
```

For each primitive determine:

1. which Vāk nodes/families genuinely describe it or its possible operations;
2. whether the relation is source-backed, architectural, implemented or proposed;
3. whether the relation applies to identity, current state, activity, or one situated use;
4. how it is exposed to humans and Agents;
5. whether a canonical Action binding exists;
6. how Activity/Return can evidence the relation.

A single O:I primitive may participate in several Vāk readings under different qualifications. The matrix must preserve this multiplicity.

## 8. Pass F — canonical Action relation

Actions are the first execution proving case.

Preserve:

```text
ActionRef
    what actual executable operation exists

VakActionProfile
    how that Action can be read within Vāk

Invocation
    situated request by actor/Agency/World/Method

Activity
    actuality unfolding

Return
    returned difference/evidence

Recognition
    interpretation/integration of what occurred
```

`VakActionProfile` should be capable of carrying:

```text
ActionRef
native owner
VakRef(s)
R-family / action relation(s)
Śiva operator affinities/roles
Śakti horizon affinities/roles
source/architecture evidence
standing
```

Actual `VakPath` is attached to or reconstructed from situated Method/Invocation/Activity evidence rather than inferred solely from the static Action profile.

## 9. Pass G — AIKit Resolve / Search / praxis integration

AIKit is the owner of how this complexity becomes usable by an actor.

### 9.1 Resolve beneath Search

Audit existing Search and V2 Resource application operations and establish `resolve` as the underlying semantic verb where the implementation supports it:

```text
address resolution
knowledge resolution
praxis resolution
capability resolution
ContextResolution
projection resolution
```

Search remains the fast human/Agent discovery projection. Do not freeze the current palette punctuation as the Vāk grammar; current `@`, `>`, `+`, `:` lanes are implementation/history to inspect during migration.

### 9.2 Search ranking / familiarity

Preserve accepted #29 semantics:

- destination familiarity;
- route familiarity with provider/lens/revision evidence;
- Project/actor/Agency/Focus context;
- deterministic recency decay;
- separate fitness signal;
- explicit forget/reset;
- learned ease has no source/trust/authority mutation power.

Extend the evidence model so a route may additionally retain a Vāk path/operation reading when QL is available.

### 9.3 Skill / SkillSet / Method relation

Preserve current AIKit praxis grammar:

```text
Skill
    reusable organised intelligent praxis

UsageOverlay
    scoped adaptation

Method
    source-addressable Focus-bearing composition of
    Skills + Actions/Capabilities + ContextSources + overlays

SkillSet
    additive repertoire / projection unit

ContextResolution
    what becomes operative here
```

Vāk path relation:

```text
Method
    durable articulated praxis relation

expected / authored Vāk path pattern
    optional semantic expression of that praxis

actual Vāk path
    observed trajectory through Invocation / Activity / Return

familiarity
    learned accessibility of real trajectories

fitness / Factory evidence
    evidence concerning efficacy/quality
```

A stable repeated path may create recognition pressure to author/refine a Method, UsageOverlay or Skill. Promotion remains an explicit authored/governed act.

### 9.4 Skill descriptions and Agent self-language

Coordinate with the current instruction-craft protocol. Develop structured capability descriptions that can answer in first-person/introspective form where useful:

```text
What can I do?
When am I relevant?
What do I need to know?
What can I act upon?
Which powers/Actions do I expose?
Which Methods relate me to a Focus?
What do I return?
When should I query another / query myself / withhold / act?
```

M0-3 statement/query/self-other forms should inform parse/generation here rather than being decorative metadata.

### 9.5 Agent first disclosure of an O:I world

The Agent-facing seed is a small projection of the whole field:

```text
identity / Agency / World / Focus
available Ground / Knowing
Original / determining structures
Reflection / meanings
Language / forms
World / story/context relations
Power / Techne / praxis
operative Śiva relation grammar
```

Each disclosed item should lead to real refs and deeper traversal rather than copied explanatory prompt prose.

### 9.6 Soft whole-suite integration

For whichever O:I products are installed, AIKit should assemble the Agent's actual field through their native refs/contributions. Product identity remains native; Vāk supplies a soft integrative reading across the assembled internality.

The six canonical O:I products are the maximal current suite specimen, not a requirement that every installation contain all six.

## 10. Pass H — runtime path and alignment observability

Define a provenance-bearing `VakPath` / `VakPathStep` reading over real activity:

```text
step id
VakExpression / VakRef(s)
O:I subject refs
MethodRef?
ActionRef?
InvocationRef?
ActivityRef?
World / Project / Focus
source Surface
start/end / result/return refs
standing / derivation provenance
```

Use the language to make internal alignment relations inspectable, including examples such as:

- Bimba/source vs Pratibimba/derived-reading drift;
- available Library vs actually disclosed Context;
- Techne/Action availability vs authority;
- Method phase vs premature expression/action;
- MonoPoly over-unification/fragmentation pressure;
- R0–R5 lifecycle imbalance;
- self-report vs world-report vs reflexive query mismatches.

These are diagnostic readings over real refs/evidence. Record both source language and observed technological facts.

## 11. Required research matrix artifacts

The programme should produce, in order:

1. `anuttara-language-map.md` — exact retained human-readable source snapshot.
2. generated exact 109-entry runtime registry + parity fixture.
3. `anuttara-vak-relations-v1` — source/source-derived graph of Vāk relations.
4. `vak-oi-primitive-relation-matrix-v1` — provenance-bearing matrix to actual O:I primitives.
5. `vak-action-profile-v1` — canonical Action↔Vāk relation contract.
6. `vak-expression-v1` — Śiva×Śakti typed compositional expression.
7. `vak-path-v1` — actual Method/Invocation/Activity path evidence.
8. AIKit Resolve/Search/Method/familiarity integration fixtures.
9. minimal + maximal Agent disclosure fixtures.
10. cross-surface conformance: CLI/TUI/structured Agent and, where appropriate, desktop.

Machine formats should be added only after each semantic relation is understood well enough to deserve a stable contract.

## 12. Minimal delivery profile

The minimum coherent operational cut is:

```text
exact 109-node source registry
+ exact source traversal/provenance
+ M0-3 statement/query/self-other typed readings
+ M0-4 O#/X#/N#/M#/#/##/R# source relations
+ M0-5 Śiva×Śakti typed expression
+ first provenance-bearing O:I primitive relation matrix
+ explicit native Action bindings
+ AIKit Vāk-aware Resolve/Search readings
+ Method optional Vāk pattern / actual path evidence
+ #29 familiarity over Vāk-aware route evidence
+ compact Agent first-disclosure seed
+ CLI/structured-Agent conformance
```

This cut should already make Vāk a real traversable and partially executable language of the O:I internal world.

## 13. Maximal delivery profile

The maximal intended form develops the same substrate rather than replacing it:

```text
complete 109-node source + derived relation graph
+ formal parse/generation across M0-3
+ explicit will/knowledge/action and self/other relational readings
+ deep M0-4 holographic contextual/refraction engine
+ compositional/nested Śiva×Śakti syntax over arbitrary typed refs
+ broad O:I primitive and lifecycle semantic coverage
+ Action / Method / Activity / Return Vāk path reconstruction
+ learned path accessibility + Factory fitness return
+ first-person introspective Skill/Method/capability disclosure
+ soft whole-suite Agent self-world model
+ alignment/drift diagnostics
+ natural-language / CLI / TUI / structured-Agent syntax projections
+ M5/Epii Recognition and governed return into the living Bimba field
```

The maximal form remains source-traversable: every sophisticated interpretation should permit return to the relevant Bimba/Vāk nodes, formulations, relation path and technological evidence.

## 14. Development order

Execute in this order unless live code evidence requires a local adjustment:

```text
A source parity
→ B M0-3 language recovery
→ C M0-4 holographic/context relation recovery
→ D M0-5 compositional grammar
→ E O:I primitive relation matrix
→ F Action execution bindings
→ G AIKit Resolve/Search/praxis integration
→ H Activity/VakPath/familiarity/return
→ minimal conformance closure
→ maximal semantic/UX development
```

Do not jump directly from the six M0-5 labels to UI syntax. The full Anuttara source provides the semantic depth that makes the compositional language worth having.

## 15. Closure

This wayfinder is closed when the full Anuttara language is preserved and traversable in QL-MEF; M0-3, M0-4 and M0-5 have explicit operative contracts; actual O:I primitives and Actions can carry provenance-bearing Vāk readings; AIKit can resolve/disclose/use those readings through Search, Methods and learned routes; actual Activity/Return can be reconstructed as Vāk paths; and both the minimal and maximal delivery profiles have concrete implementation/evidence matrices rather than remaining conceptual prose.
