# K / L5 M′ Continuity Ledger — backcheck over landed work

**Standing:** continuity ledger, 2026-09-17. Programme owner: QL-MEF **#201** under **#135**.
**Purpose:** backcheck, not redesign. It recovers what the accepted K8–K10 work actually landed and how the owner-ratified L5 Technē dual-reading (`docs/L5-TECHNE-DUAL-READING-LOCK.md`) now locates it, per Q1 of `.wayfinder/maps/k-aw-expression-production.md`. It does not reopen kernel architecture, does not reinterpret M′ as six generic view modes, and does not invent an Expression architecture.
**Audited revision:** branch `session/k-aw-expression-production-2026-09-17`, HEAD `e55bf0224ea6113efd042c8f3146536cef31f147` (= `origin/main` `3c48665` + the corrected execution map docs + the carried T0 Technē contract merge `c41c75d`; 0 commits behind main). All file/line citations are against this HEAD.
**Read-only side inspections:** open PR **#224** (head `techne/tb0-connective-base`, TB0 commits `8781476` + `2c704dc`) and its integration successor branch `techne/tb0-surfaces-port` (TB0 + six M′ instrument lanes + X3, 136 files, in-flight, unmerged). **Audit is of main; those branches are cited as pending intent only.**
**Coordination state:** #212–#220 are the OPEN instrument tracks; #223 (merged `dc4d226`) landed Geometry propagation into Wiki; #196 (merged `4276b94`) landed the Nara activity/AW3 join; #197 (merged `3a3d7db`) is the owner-approved Expression Field projection contract; #199 (merged `2848f7e`) exposed the K10 Nara reading to the Expression host; K8 = `a15b01e` (#176 line), K9 = `fa97f83`, K10 = `4db001c` (#189), K5 #150 `bb32250`, K6 #148 `c421080`, K7 #151 `276f23e`, Vāk #142 `374cf8d`, AW1 `05b6d4a`, K8.0/AW2–AW3 `a30903b`.

The one M′ field carries two application readings over the same subject/refs/sources/constellations/Actions. Per the lock: **4:2** = M0′ Project/Wiki/Graph, M1′ Canvas/Constellation, M2′ Relation Field/Timeline, M3′ Journey/Scenes, M4′ Places/World, M5′ Palace; **3:3** = the lived Expression reading (M1+M2+M3 cosmic body → M4 lived/Nara, with M0 graph/source and M5 Epii/Agent as summonable depths). Crossing preserves subject identity, source basis, constellation identity and native Action ownership.

---

## M0′ — Project / Wiki / Graph (ground and address)

1. **M/Bimba domain relation.** M0 Anuttara: the actual Bimba map and source ground. Registry `fixtures/kernel/m-tree-v1.json` (`ql.m-tree/v1`, `registry_revision 259a2f49…`, 1,876 nodes / 21,083 relations / 6 roots; 1,875 coordinates) compiled natively (`crates/ql-mef/src/m_tree.rs:15-19`, `NATIVE_M_MANIFEST` include + build-time `m-tree-v2.json`). Source lock `data/epi-bimba-map/source-lock.json` pins C-Experiments `daa660cb…`, map root `Idea/Bimba/Map`, per-file git-blob identities. Live Neo4j capture: 2,141 coordinate nodes / 11,774 relations (`docs/kernel-rebuild/K4-M123-CENSUS.md`, `fixtures/kernel/census/live-bimba-capture-v1.json`). Branch structures, alternate-notation groups, source records and parent discrepancies are registry data (`parent_discrepancies`, `alternate_notation_groups` keys in the fixture); the live-only `M0'`–`M5'` deep-instrument primes are deliberately never joined onto `#N` roots (census tests pin this).
2. **M′ instrument.** 4:2 office: locate/traverse/establish the working world (lock §6) — explicitly the deep aperture over existing Central/ProjectCentral + AIKit Wiki/ProjectMap, not a new project store. 3:3 relation: the Expression graph/source depth — bounded local whole summoned around the lived Expression.
3. **Current implementation/read model.** Rooted traversal `ql.aw1-rooted-m-world/v1` (`crates/ql-mef/src/aw1_world.rs:13,119` — taproot `#`, master `M`, direct/conjugate `RootedFace`, native-owner dispositions); coordinate/relation index `crates/ql-mef/src/m_map.rs:82,239,278` (`MCoordinate.canonical_ref`, `MRelation`, `MMapIndex`); capability/locate/refract provider `crates/ql-wiki/src/registry_provider.rs:28-66` (`RegistryDisclosureProvider`, operations Capabilities/Locate/Refract, forms sixfold/4+2/direct-conjugate); service host `crates/ql-cli/src/lib.rs:927`; ledger machinery `fixtures/kernel/m-ledger-v1.json` (1,915 rows / 2,401 implementations / 588 discrepancies) with `scripts/m-ledger.py check` and `ql kernel validate-ledger`; census receipt `fixtures/kernel/k8-census-receipt-v1.json` (2,045 constructs, 622 reviewed K8).
4. **3:3 Expression relation.** `docs/kernel-rebuild/EXPRESSION-FIELD-PROJECTION.md` §3: selected coordinate/Wiki subject → bounded local whole → typed relation/source/property readings → optional QL constellation/harmonic/glyph grammar → O:I Expression entities. Contract surface: `TechneWhole` (`member_refs`/`relations`/`focus_refs`, `crates/ql-adapters/src/techne.rs:94-102`) and `ground_ref()` as traversal step 0 (`techne.rs:500-505`, test `the_ground_ref_is_step_zero_of_the_traversal`).
5. **4:2 Technē relation.** `TechneInstrument::Project` (`techne.rs:22-30`); representative fixture discloses project `available, m_prime 0, "4:2-deep"` (`fixtures/techne/representative-subject-v1.json`). `ql.techne/v1` floor landed (schemas `schemas/techne/ql-techne-{reading,session}-v1.schema.json`). TB0 (#224, pending) adds `whole_ref`/`project_ref`/`world_ref` + `cross_cut()` to `DisclosureSession`.
6. **Profile / Scene / Action bindings.** `NativeActionRef` routing-only (`techne.rs:362-372`; adapter `route_action` returns the owning authority without executing, test `fixture_adapter_routes_actions_to_native_owners_without_executing`). `ExpressionBinding` carries `oi:expression:*` refs + profile (`fixtures/techne/representative-subject-v1.json`). Runtime Wiki→Expression materialisation is O:I **#366**'s gate (#201 comment 2026-09-16T18:00Z): Atlas generation must consume it, not mint detached files.
7. **Native owner.** QL-MEF: M0 semantics/registry/ledger/constellation overlays. Central: root World, Projects/ProjectCentral, DAY/NOW. AIKit: ProjectMap/SemanticWiki/Search/KnowledgeRoute. O:I: graph rendering + Expression EX3 projection (lock §18, §20).
8. **Shared subject/source/relationship refs.** `MCoordinate` canonical refs (`#`-paths), `SourceRecordRef` + registry revision, relation origin/`origin_ref` (`WholeRelation`, `techne.rs:81-89`), `subject_ref`/`native_owner`/`native_revision` verbatim (byte-exact round-trip test), constellation/shape refs (`ql:shape:1.1.0:*`).
9. **Accepted evidence.** K0–K4 receipts (#124–#128; K4 `5b24b95d…`, #153); AW1 `05b6d4a`; T0 contract on this HEAD (`2159c7d`, `c83770f`, `c41c75d`); tests: `native_m_tree`, `registry_fixture`, `m_ledger` suites green in workspace runs; `python3 scripts/m-ledger.py check` → OK (run this audit).
10. **Remaining gap.** Adapter: the TB0 connective-base additive fields exist only on open PR #224 — main's reading cannot yet express relation standing/source/evidence or session world/project/return refs (fixture-only until merged). Owner defect (cross-repo, by design): the M0′ convergence surface itself is #213 + O-I #366 (Central/AIKit/O:I); no QL-side store/search/graph duplication exists or is permitted.

## M1′ — Canvas / Constellation (constructive determination)

1. **M/Bimba domain relation.** M1 Paramaśiva as formal/harmonic/topological constitution, plus the QL constellation/geometry morphology of the whole field: canonical shapes (`crates/ql-core/src/shape.rs` — `QlShape`, contract version `1.1.0`; 4×4×4, 6×6, relational sixfold, 18-fold, 10×10 Second-Spanda, constellation grains anchor-only→twelvefold, 4×4 family pairs), the M1 engine (`crates/ql-mef/src/m1_engine.rs`, contract `docs/KERNEL-M1-ENGINE-CONTRACT.md`: all 43 M1 coordinates reconciled), Second-Spanda matheme (`crates/ql-mef/src/matheme.rs`, #206).
2. **M′ instrument.** 4:2 office: construct/arrange/bound/annotate/relate the selected field (lock §7); QL geometry may propose lawful layouts while authored arrangement stays presentation state. 3:3 relation: M1 topology/geometry/material Expression capacity.
3. **Current implementation/read model.** Shape ref resolver `crates/ql-core/src/shape_ref.rs:10-63` (#222/#223 — exact versioned refs only, aliases/unknown versions/anonymous grains rejected); Wiki structural field validation `crates/ql-wiki/src/refraction.rs:127-160` (`WikiStructuralField`: pair/D1/D2/D3 operator+coordinate law — the source-backed structural overlay a canvas may disclose); geometry constellation resolution tests `crates/ql-core/tests/geometry_constellation_resolution.rs` (6 pass); census dispositions `c/registry/promotions/k9-bindings-v1.json` (shape.rs and shape_ref.rs cross-coordinate #1–#3).
4. **3:3 Expression relation.** `WarrantedQlReading` carries `shape_ref`/`constellation_ref`/`harmonic_reading`/`geometric_reading` and is expressible **only** with a warrant (`QlWarrant` = result_class + evidence_refs + provenance_ref, `techne.rs:107-151`; test `ql_facet_without_warrant_is_rejected`). Expression projection doc §4 M1: accepted M1 state remains numerical/formal owner; the point-cloud body is a presentation target.
5. **4:2 Technē relation.** `TechneInstrument::Canvas` (m_prime 1, 4:2-deep in fixture). Constellation membership composition must go against the real constellation owner (lock §7); the adapter exposes, never owns, constellation identity.
6. **Profile / Scene / Action bindings.** #223 shape binding reaches Wiki readings as `ql_form_refs`/`operator_refs` + the `ql-mef/shape-binding/v1` extension (`crates/ql-wiki/src/shape_binding.rs:209-253`, `ShapeAwareWikiRefractionEngine::refract`); `ExpressionBinding.profile_ref` present in fixtures; QL-backed layout/profile grammar into O:I expressions is #201 TA2 / O:I #214 territory.
7. **Native owner.** QL-MEF: QL geometry/constellation/harmonic semantics. O:I + Research Canvas: mature canvas interaction (lock §18). Research Canvas interaction code is migration/source technology.
8. **Shared subject/source/relationship refs.** Versioned shape refs (`ql:shape:1.1.0:…`), canonical operator refs (`RELATIONAL_SIXFOLD_OPERATOR_REF`, compression refs), member `position`/`face` (0..5, direct/conjugate), `basis_refs`, `whole_ref` — all carried verbatim through Wiki → readings → techne reading.
9. **Accepted evidence.** Geometry #204/#206 (k9-bindings census rows), #222/#223 merged `dc4d226`; tests run this audit: `geometry_constellation_resolution` 6/6, `shape_binding_refraction` 4/4, `vak_composition` 11/11, `compressed_geometry_vak` 1/1.
10. **Remaining gap.** Adapter gap: no production QL `TechneAdapter`/`FocusedInstrumentSource` is registered against live Wiki subjects yet (the recorded T9 open item; today only the K9 dev-walk harness and test fixtures exercise it). Owner defect (cross-repo): the canvas surface itself is O-I #214 (open).

## M2′ — Relation Field / Timeline (becoming and efficient relation)

1. **M/Bimba domain relation.** M2 Paraśakti: spectral/material/resonant differentiation — `ql.m2-engine/v1` (`crates/ql-mef/src/m2_engine.rs:80` `WorldObservation`), M2-0…M2-5 differentiated Power field (`docs/kernel-rebuild/m2-engine-v1.md`), correspondences `fixtures/kernel/m2-correspondences-v1.json`, music/music_completion (`music.rs`, `music_completion.rs`), Vimarśā (`m2_vimarsha.rs`), planetary/chakral synthesis; K6 receipt #148 `c421080`.
2. **M′ instrument.** 4:2 office: disclose movement, causation, recurrence, resonance, opposition, inheritance, transformation (lock §8); Timeline is its strongest ordinary projection, not its definition. 3:3 relation: M2 resonance/material Expression capacity (music/colour/cymatics).
3. **Current implementation/read model.** Temporal facet family preserving occurrence/receipt/valid/source/day/now/session/run/generation/presentation as distinct kinds with carriers (`techne.rs:155-225`, `TemporalKind`), semantic carrier law enforced in `TechneReading::validate` (`techne.rs:454-467`, no empty temporal facet); development-day fixture `fixtures/techne/development-day-v1.json` (test `development_day_fixture_preserves_occurrence_receipt_and_continuity`); G2 gate proven against **live Central ctrl data** (day `central:day:control:root:2026-09-15`, active NOW).
4. **3:3 Expression relation.** Snapshot exposes `m2_presentation` value plus `NaraResonanceStationDisclosure` keeping centre identity ≠ cymatic station identity (`focused_instrument.rs:470-476`); M2 contributions reach each Nara centre as `WorldContribution` (`focused_instrument.rs:442-451`); K8 joins both source-derived musical inputs on one native instrument (`a15b01e`).
5. **4:2 Technē relation.** `TechneInstrument::Timeline` (m_prime 2). Non-chronological relation standing (INSTANTIATES/ECHOES ≠ dates, `derivation_ref`, owner-supplied `confidence`) is the TB0 additive delta — `WholeRelation.standing/source_ref/evidence_refs/temporal_facet_ref` (#224, pending on main).
6. **Profile / Scene / Action bindings.** Run/session/day continuity refs ride `TemporalFacet` (`day_ref/now_ref/session_ref/run_ref`); mutations never via the reading — `NativeActionRef` routing only; schema pins ActionRoute (`schemas/techne/ql-techne-session-v1.schema.json`).
7. **Native owner.** QL-MEF: M2 producers + warranted QL relation semantics. Central: DAY/NOW. Factory: Runs/evidence (wall-clock absent by design — revision/cursor ordering). O-I: timeline surface (#215).
8. **Shared subject/source/relationship refs.** `facet_ref` (resolvable temporal identity), `source_ref`+revision, day/now/session/run refs, relation `temporal_facet_ref` (TB0) distinguishing dated instance from trans-temporal archetype.
9. **Accepted evidence.** K6 #148; K8 #176 `a15b01e`; techne contract 12/12 (this audit); techne-gates G2 pass (live), G-T pass.
10. **Remaining gap.** Adapter: relation-standing/attempt/return temporal continuity fields are fixture-only until #224 merges. Owner defect (cross-repo): no live Factory-run temporal producer (Factory carries no wall-clock; ProjectCentral `now/agents` handoffs are the join) and ai-kit has no temporal/spatial facets beyond `aikit.techne-facet/v1` declared extensions — T1 successor work (#215 lane).

## M3′ — Journey / Scenes (formal mediation between 4:2 and 3:3)

1. **M/Bimba domain relation.** M3 Mahāmāyā: form/inscription/clock — nucleotide/pair/biquaternion, 64/384/472, I-Ching/Tarot/DNA-RNA, fold/pose, 360/24/720 coupled clock (`docs/KERNEL-M3-ENGINE-CONTRACT.md`, `crates/ql-mef/src/m3_engine.rs`, `m3_state.rs`, `m3_clock.rs`, `m3_source.rs`); K7 receipt #151 `276f23e`; M3-0 realised at existing `M3-5-5/0` (aperture contract, #160).
2. **M′ instrument.** 4:2 office: turn researched determinations into an authored traversal capable of becoming lived Expression (lock §9) — the strongest formal hinge. Persistence law locked: Journey → **real Expression Scene refs**; no second scene ontology.
3. **Current implementation/read model.** K9 focused instrument (`crates/ql-mef/src/focused_instrument.rs`, `ql.focused-instrument/v1` + five companion contracts, lines 20-25): `ClockDisclosure`/`ClockPresentation` (assemble/explode per pair, clock axis), `FieldTarget` constituents, `VakExpressionBinding` (`ql.focused-instrument-vak-expression/v1`, media literal/glyph/image/geometry/sound/trajectory, lines 80-167), `VakPerformanceSummary` consuming AW2/Factory performance events (`vak_performance.rs`), snapshot `focused_instrument.rs:622-641`; bounded host `crates/ql-mef/src/bin/ql-focused-host.rs`; browser driver `adapters/retained-field/focused-instrument-session.mjs` (+ tests) from #199.
4. **3:3 Expression relation.** Journeys sequence real Scenes: `ExpressionBinding { expression_ref, revision, scene_ref, composition_ref, profile_ref }` (`techne.rs:347-357`); fixture binding `oi:expression:l5-techne-constellation/scene/opening`. G4 gate: reorder proposed through the Expression owner's native Action; cue binds subject refs byte-verbatim; **no shadow persistence**.
5. **4:2 Technē relation.** `TechneInstrument::Journey` (m_prime 3). Scene framing (time+place+subjects+source refs) stays with/references the actual Scene.
6. **Profile / Scene / Action bindings.** Scene refs are O:I substrate identity (`oi.expression/v1` owner); QL supplies source-backed selection, Vāk forms, clock/form packets; reorder/compose = owner native Actions disclosed in `actions[]`.
7. **Native owner.** QL-MEF: M3 producer + Vāk expression forms. O-I: Scene/Expression/presentation substrate (#216 lane, open).
8. **Shared subject/source/relationship refs.** `scene_ref`, `expression_ref`+revision, subject refs, source selectors, Vāk binding refs (source coordinate + expression identity + selected reading).
9. **Accepted evidence.** K7 #151; Vāk #142 `374cf8d`; #199 `2848f7e` (host exposure); focused-instrument in-module tests (`focused_instrument.rs:1001+`) green in workspace; G4 gate pass.
10. **Remaining gap.** Adapter: main's T0 fixtures carry one Expression binding but no multi-scene Journey specimen; the TB0 conformance fixture (#224) adds the Journey-scene case. Owner defect (cross-repo): Journey authoring surface is O-I #216 (open). No QL defect.

## M4′ — Places / World (situated whole and Earth relation)

1. **M/Bimba domain relation.** M4 Nara: the lived personal world received through the shared celestial occasion — `ql.nara-personal-field/v1` (`crates/ql-mef/src/nara.rs`), complete M4 domain `ql.nara-m4-domain/v1` with all six branches (`crates/ql-mef/src/nara/domain.rs`; `docs/kernel-rebuild/K10-NARA-M4-CONTRACT.md`), personal coupled receiver `ql.personal-coupled-session/v1` (`crates/ql-mef/src/continuous/personal.rs:22,27`), K8 sky/planetary condition (`docs/kernel-rebuild/K8-SKY-CONTRACT.md`, `k8_sky_numeric` tests, `WorldObservation`) as the wider reference-frame depth. World/solar/geography/Nara scale chain per lock §10; geography relation types stay non-interchangeable.
2. **M′ instrument.** 4:2 office: situate the field in the concrete world context (lock §10); Earth-facing body is the map/street/globe work; consumes current M2/M3/M4 producers, never recomputes astronomy in the UI. 3:3 relation: Nara/EarthBody/current situated Expression — M4 is the hinge of the whole field.
3. **Current implementation/read model.** `NaraExpressionSession` (`ql.nara-expression-session/v1`, `focused_instrument.rs:492-510`): exactly seven accepted centres as `NaraCentreExpressionReading` (locus_ref `ql:nara:<subject>:centre:<n>`, ordinal, label, `SourceRevision`, m1/m2/m3 `WorldContribution` refs, resonance) + distinct `NaraEarthBodyExpressionReading` + `NaraResonanceStationDisclosure` + portable cues (`ql.nara-expression-portable-cues/v1`: safe refs/cue refs only). Projection contract `ql.nara-expression-projection/v1` with `EXPRESSION_HOST_OWNER: "oi"`, `BimbaSelectionBinding`, `EpiiSessionBinding`, `SharedPresenceConsent`, protected state ref (`crates/ql-mef/src/nara/expression.rs:18-19,114-183,252-276`). Activity ground `ql.nara-activity/v1` (`crates/ql-mef/src/nara/activity.rs:14`): `CentralTemporalRefs`, span-anchored parse receipts, late-return flag (`is_late_return`, line 249), AW3 thought-consumption join (#196). Place facet: temporally valid identity, precision/hierarchy/validity, `unlocated` truthful (`techne.rs:229-299`).
4. **3:3 Expression relation.** #199 exposed exactly this reading to the Expression host: protected portable cues, never raw identity/journal/bioquaternion/centre bodies (projection doc §4 M4); `NaraExpressionSession` rides the focused snapshot (`nara_expression` field) with `current`/standing.
5. **4:2 Technē relation.** `TechneInstrument::Place` (m_prime 4). TB0 adds `PlaceFacet.relation` in native vocabulary verbatim (`OCCURRED_AT`/`LOCATED_IN`/`OPERATED_IN`/`TRAVELLED_TO`/`MYTH_LOCATED_AT`) + `uncertainty` (#224, pending).
6. **Profile / Scene / Action bindings.** `action_refs` on `NaraExpressionSession`; consent/disclosure/lifecycle in `SharedPresenceConsent`/`ExpressionDisclosure`; multi-Nara `crates/ql-mef/src/nara/multi.rs` (protected derivative state, deliberate shared presence). Voice/deixis semantics are #201 TA3 with native owners Actuation #91 / AIKit #317 / O-I #336.
7. **Native owner.** QL-MEF: Nara/M4 meaning + protected projection. Central: DAY/NOW consumed via source-qualified temporal handles. Actuation: Agency; AIKit: voice body; O-I: desktop/World presentation; Workcell: material execution (lock §18).
8. **Shared subject/source/relationship refs.** `subject_id`, `event_ref`, `occasion_ref`, `profile_generation`/`personal_reception_generation`, centre locus refs, EarthBody `frame_ref`, `protected_state_ref`, `SourceRevision` list — all preserved across crossings; `place_ref` identity independent of coordinates.
9. **Accepted evidence.** K10 #189 `4db001c`; #196 `4276b94`; #199 `2848f7e`; tests this audit: `nara_personal_field` 1/1; K8 numeric sky tests in workspace green; G3 partially proven (one historical place case via contract + lane suite).
10. **Remaining gap.** Adapter: **G3 open** — no second, non-historical real spatial producer emits `PlaceFacet`s (ai-kit has no spatial facets; T1 successor). Live ephemeris (Kerykeion installation, provider revision binding) is the K8.1 owner-machine obligation — numeric fixtures landed, live join unproven in-repo. Owner defect (cross-repo): World/Places surface is O-I #217 (open, ported in-flight on `techne/tb0-surfaces-port`).

## M5′ — Palace / integral articulation (inhabitable whole and Return)

1. **M/Bimba domain relation.** M5 Epii: the canonical agent's investigation/articulation/pedagogy/Return over the same field; M4.5 as Nara's inner learning; S5 self-reference seat `ql.aw1-s5-self-reference/v1` (`crates/ql-mef/src/aw1_self_reference.rs:12-20`); T/T′ consumption owners `factory.run-thought-consumption/v1` + `ql.vak-thought-consumption{,-projection}/v1` (`crates/ql-mef/src/vak_thought_consumption.rs:21-27`); recognition/Return `ql-mef/meta-recognition/v1` (`crates/ql-wiki/src/recognition.rs:8-163`, `RecognitionLedger`, `RecognisedMetaAmendment`).
2. **M′ instrument.** 4:2 office: make the articulated field inhabitable/memorable/teachable and close into Return (lock §11) — composition state over native refs; no second graph or scene store. 3:3 relation: Epii/Agent depth aperture around the Expression.
3. **Current implementation/read model.** In QL-MEF: recognition ledger, thought-consumption projections, AW3 reception (`crates/ql-mef/src/nara/activity_aw3.rs`), Return refs carried on shape bindings (`return_refs`, `crates/ql-wiki/src/shape_binding.rs:57`), `EpiiSessionBinding` in the Nara projection, agent aperture contracts in `vak_oi.rs` (O:I resource vocabulary incl. `Journey`, `Agency`, `AgentSession`, `SharedField`). **No Palace surface exists in QL-MEF crates** — by ownership that is O-I/Research Canvas mature-interaction territory (#218 open).
4. **3:3 Expression relation.** Epii inspects/operates Expressions through O-I's agent-native operations and native Actions (projection doc §4 M5); co-reference through one `DisclosureSession` + `agent_session_ref` (`techne.rs:587-603`; G6 gate pass).
5. **4:2 Technē relation.** `TechneInstrument::Palace` (m_prime 5). In the TB0 specimen it is deliberately **unavailable** with an explicit reason — truthful absence as data. The canonical traversal's last leg is a Return: G-T gate proves the Return leg carries a governed-write native Action (`aikit.wiki.stage`).
6. **Profile / Scene / Action bindings.** Palace composition of real Expressions routes the Expression substrate's own `scene_compose` change to the disclosed owner action (G5 pass); arrangement is presentation-only; identity byte-verbatim.
7. **Native owner.** QL-MEF: M5 semantics, Vāk, T/T′ interpretation, recognition candidates. Actuation: Agent/Agency/authority/Return. Factory: Runs/evidence. O-I: Palace/composition surface. AIKit: Skill/Method/praxis reuse (lock §18).
8. **Shared subject/source/relationship refs.** `return_refs` (shape binding → reopened basis), `derivation_ref`, `return_target_ref` (TB0), `agent_session_ref`, recognition candidate refs with owner-registered final authority (Nara never silently registers QL recognition candidates — K10 contract §1).
9. **Accepted evidence.** AW3 `a30903b`; AW2 `3a2307d`; K10 `4db001c`; G5/G6/G-T gates pass; techne contract 12/12.
10. **Remaining gap.** Adapter: TB0 `agency[]` floor (guardian/anima/aletheia/techne roles with `m_index`/`authority`/`privacy` law) and `DisclosureSession.cross_cut()` exist only on #224. TA4 `EpiiEnrichment` delegation contract (#201) not yet implemented in QL-MEF. Owner defect (cross-repo): Palace surface + composition UX is O-I #218 (open); the existing Palace UI survives only if real interaction testing confirms the office.

---

## Propagation verification — Bimba → M′ (owner's special attention)

1. **Coordinates and branch structures.** The registry is the single coordinate field: `fixtures/kernel/m-tree-v1.json` (source-locked `daa660cb`, registry revision `259a2f49`) → compiled into `ql-mef` (`m_tree.rs:15-19`) → resolved through `MMapIndex` and `resolve_rooted_m_world` (taproot `#`, master `M`, direct/conjugate faces). Branch traversal is parent/children/depth in the registry; aliases join only through the registry's own alternate-notation groups; deep-instrument primes (`M0'`–`M5'`) never impersonate `#N` roots (K4 census law, test-pinned).
2. **Source relations.** `MRelation`/`MRelationClass` with source/endpoint refs and implementation bindings feed the m-ledger (rows/assessments/evidence/discrepancies with distinct standing); at the instrument boundary they surface as `WholeRelation { relation, from_ref, to_ref, origin, origin_ref }` and as typed Wiki relations — vocabulary preserved, never relabelled (`techne.rs` doc comment and schema).
3. **Constellations and shapes.** Constellation identity is `QlShape`/`ConstellationGrain` with versioned refs; Wiki transports them as an optional validated extension (`ql-mef/shape-binding/v1`) and copies the exact view into every returned reading's `ql_form_refs`/`operator_refs`.
4. **Placement/layout can never manufacture graph relations — the #222/#223 mechanism** (`crates/ql-wiki/src/shape_binding.rs:119-176`, `validate_for_target`):
   - the binding's `subject_ref` must equal the stable Wiki target ref;
   - every shape member must be retained in `basis_refs` (members are actual disclosed subjects, not invented points);
   - a one-fold presentation with disclosed members is valid **only** with a canonical compression operator (`ql:shape:1.1.0:compression:3-to-1-recognition` or `…:disclosed-to-0-1`) whose operator **and** derivation ref exactly match `QlShapeCompression::to_onefold` computed from the actually disclosed member grain — a claimed 2/3→1 recognition must reopen to a real basis (`crates/ql-core/src/shape.rs:16-18,149-151`);
   - a presented constellation grain must equal the grain computed from the actual members (`form.grain() != expected → refuse`);
   - upstream, `WikiStructuralField::validate` pins pair/D1/D2/D3 operator+coordinate identity, and the techne layer makes unwarranted QL metadata **inexpressible** (`QlWarrant` required) and refuses view/layout state riding readings or sessions (tests `ql_facet_without_warrant_is_rejected`, `view_state_cannot_ride_the_reading_or_the_session`).
5. **Main M subsystems → M′ instruments.** M1/M2/M3 propagate into the K9 focused snapshot as presentation values over one `PersonalCoupledSession`/`CoupledBasis` with exact `EventBasisRefs` and AW2 currentness standing (Current/Stale/Missing, `focused_instrument_aw2.rs:24-40`); M4 propagates as seven independently sourced centre readings + EarthBody with per-centre m1/m2/m3 `WorldContribution` refs; M5 propagates as agent aperture + T/T′/recognition; M0 propagates as the grounded whole (`ground_ref`) and Bimba selection (`ql.focused-instrument-bimba-selection/v1`, opaque source-qualified identities K9 never reconstructs). `InstrumentFocus` covers M1–M5 only; M0/M5 are apertures, matching the corrected projection doc §8.

## Exact gap list

| id | M′ | What is missing | Native owner | Class | Smallest concrete next action |
|---|---|---|---|---|---|
| GAP-1 | all | TB0 connective-base additive fields (relation standing/source/evidence/temporal-facet/derivation/confidence; PlaceFacet.relation+uncertainty; attempt/return refs; `m_coordinate_ref`/`return_ref`; `application_cuts[]`; `agency[]`; session `whole/project/world/context_frame/occasion/return_target` + `cross_cut()`) are on open PR #224 (`techne/tb0-connective-base` @ `2c704dc`), not on main | QL-MEF #212 | adapter omission (review pending) | Owner review/merge of #224; then #213–#219 fan out against TB0-1, tag `ql.techne/v1`, head `2c704dc` |
| GAP-2 | M0′ | M0′ Project/Wiki/Graph convergence surface (LIST/TREE/GRAPH over Central World + AIKit ProjectMap/SemanticWiki + O:I graph) and the runtime Wiki→Expression materialisation gate | O:I #366 + AIKit/Central; QL track #213 | owner defect (cross-repo, by design) | Execute #213 from TB0 + O-I #366; QL contributes constellation/shape overlays only — no store/search duplication |
| GAP-3 | M1′ | No production `TechneAdapter`/`FocusedInstrumentSource` registered over live Wiki subjects (only K9 dev-walk harness + fixtures); recorded T9 open item | QL-MEF (adapter) | adapter omission | Register one production source over `RegistryDisclosureProvider` + `ShapeAwareWikiRefractionEngine`; prove one real subject G1-style Graph→Timeline→Graph with byte-exact refs |
| GAP-4 | M2′ | No live non-Central temporal producer: Factory Runs carry no wall-clock (ProjectCentral `now/agents` handoff join only); ai-kit temporal facets are declared extensions, not live feeds | Factory / AIKit (T1 successor) | owner defect (cross-repo) | T1: replace development-day fixture feeds with real ProjectCentral/AIKit providers; keep occurrence/receipt labelling of native fields |
| GAP-5 | M3′ | No multi-scene Journey specimen on main (single ExpressionBinding only); Journey authoring/reorder surface unbuilt | O-I #216 (QL fixture gap closed by TB0 fixture) | adapter (fixture) + owner defect | Land TB0 fixture via GAP-1; then #216 lane builds Journey UX over real Scene refs + owner reorder Action |
| GAP-6 | M4′ | G3 open: no second, non-historical real spatial producer of `PlaceFacet`s (ai-kit has no spatial facets). Live Kerykeion ephemeris installation/provider-revision binding unproven in-repo (numeric fixtures landed) | AIKit (spatial facets); owner machine (K8.1 live sky) | owner defect (cross-repo) + owner-machine test | T1 spatial facet producer for one project/field case; owner-machine Kerykeion run per K8-SKY-CONTRACT |
| GAP-7 | M5′ | TA4 `EpiiEnrichment` delegation contract absent from QL-MEF; Palace facet truthfully unavailable (no producer); Return leg proven only at gate level (`aikit.wiki.stage`) | QL-MEF (#201 TA4); O-I #218 (Palace); Actuation (Return authority) | adapter omission + owner defect | Implement TA4 enrichment contract bound to basis revisions; prove one Palace composition → governed-write Return receipt once #218 lands |
| GAP-8 | cross | O-I TS mirror (T0 `desktop/cradle/src/techne/contract.ts`) rejects additive TB0 fields until updated | O-I (#219/#220 path) | owner defect (cross-repo) | Mechanical field-addition update of the TS mirror in the #219 lane after GAP-1 |

Non-gaps noted for continuity: `InstrumentFocus` lacking M0 is by design (M0/M5 are apertures, projection doc §8); the six 4:2 surfaces living outside QL-MEF crates is ownership-correct (lock §18), and the in-flight ports on `techne/tb0-surfaces-port` (136 files, incl. M0′ project register/surface, M2′ timeline lanes/relations, M4′ world, X3 crosscut suites) are pending, unmerged work — audited read-only, not claimed.

## Evidence run this audit (exact commands)

| Command | Result |
|---|---|
| `cargo test -p ql-adapters --test techne_contract` | 12 passed / 0 failed |
| `cargo test -p ql-wiki --test shape_binding_refraction` | 4 passed / 0 failed |
| `cargo test -p ql-mef --test nara_personal_field` | 1 passed / 0 failed |
| `cargo test -p ql-mef --test l5_projection` | 6 passed / 0 failed |
| `cargo test -p ql-mef --test vak_composition` | 11 passed / 0 failed |
| `cargo test -p ql-mef --test compressed_geometry_vak` | 1 passed / 0 failed |
| `cargo test -p ql-core --test geometry_constellation_resolution` | 6 passed / 0 failed |
| `python3 scripts/m-ledger.py check` | OK (schema, matrix/source inventory, evidence locks) |
| `python3 scripts/check-ux-spine.py --self-test` | OK (18 local links; scope: source coverage only) |
| `python3 scripts/techne-gates.py` | G0 pass ×3, G1 pass, G-T pass, G2 pass (live Central ctrl), **G3 open** (second real spatial producer), G4 pass, G5 pass, G6 pass, G7 pass (live `ql` service), G8 owner |
