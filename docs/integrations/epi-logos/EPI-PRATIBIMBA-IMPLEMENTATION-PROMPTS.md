# Epi-Logos / Pratibimba Implementation Prompts

These prompts descend from `EPI-PRATIBIMBA-BUILD-ORDER.md` and QL-MEF #30/#33/#34/#35/#36.

They are intentionally sequential. Do not skip ahead merely because a deeper formal subsystem is interesting.

Every session must re-read the actual current repository heads and open work before editing. Pinned SHAs in R2 are evidence/provenance only, not permission to ignore concurrent changes.

---

# PROMPT A — O:I EPI MODE + REAL KERNEL / PRIMITIVE BRIDGE

Work directly in GitHub across the **ACTUAL CURRENT LIVE STATE** of:

**PRIMARY APPLICATION HOST**  
https://github.com/EpiLogos/O-I

**EPI SOURCE / COMPUTATIONAL BODY**  
https://github.com/EpiLogos/Epi-Logos-C-Experiments

**FORMAL SUBSTRATE**  
https://github.com/EpiLogos/QL-MEF

**NATIVE AGENCY / APPLICATION SUPPORT — READ AND MODIFY ONLY AS ACTUALLY REQUIRED**  
https://github.com/EpiLogos/ai-kit  
https://github.com/EpiLogos/Actuation  
https://github.com/EpiLogos/Workcell  
https://github.com/EpiLogos/Central  
https://github.com/EpiLogos/agent-system-design

## START FROM CURRENT AUTHORITY

Read before implementation:

- O:I `docs/positions/FOUNDING-POSITIONS.md` or canonical successor;
- O:I #23, #25 and #26 and the current `desktop/` implementation;
- QL-MEF #30 and:
  - `docs/integrations/epi-logos/EPI-PRATIBIMBA-BUILD-ORDER.md`
  - `docs/integrations/epi-logos/pratibimba-build-order.json`
  - `docs/integrations/epi-logos/EPI-LOGOS-DEVELOPMENT-WAYFINDER.md`
- QL-MEF #33;
- Epi:
  - `Idea/Bimba/Seeds/M/M'-SYSTEM-SPEC.md`
  - `Idea/Bimba/Seeds/M/M4'/M4'-SPEC.md`
  - `Idea/Bimba/Seeds/M/M'-TAURI-PORT-SPEC.md`
  - current S4/S5 master specs for Ta-Onta/Epii boundaries;
- actual current code in:
  - `Body/S/S0/epi-lib`
  - `Body/S/S0/portal-core`
  - Nara journal/personal-identity code paths.

Where older Epi shell documents specify Theia/Electron/dedicated-Tauri as the generic host, treat the **experience/function design as retained** but the generic shell topology as superseded by current O:I desktop architecture. Do not discard useful renderer or interaction work.

## DESTINATION

Create the smallest real foundation for an **O:I-hosted Epi / Pratibimba mode**.

The relation is:

```text
existing O:I Rust desktop / Surface host
        ↓
native Epi/Pratibimba contribution or mode
        ↓
small Epi-owned typed bridge
        ↓
existing epi-lib + portal-core computation
```

This tranche is **NOT** a new Epi desktop, NOT a full M0–M5 rewrite, and NOT an M1/M2 feature build.

## O:I UX / HOST DISCIPLINE

Preserve the already-defined spatial O:I agent-IDE grammar rather than inventing another dashboard:

```text
global navigation / search / projects-apps
        │
        ▼
primary canvas  ───────── right situated/root-agent + context region
        │
        └──── optional lower drawer: trajectory / terminal / events / processes / logs
```

The O:I host owns generic:

- lifecycle and local privileged Rust boundary;
- application navigation and Surface regions;
- SessionSpace / AgentSession / model/harness presentation;
- terminal/process/trajectory/event presentation;
- generic capability/package/contribution mechanics;
- stable-ref co-reference between human canvas and agent.

Do not recreate those inside Epi.

## IMPLEMENT THE MINIMUM EPI PRIMITIVE BRIDGE

Prefer **adopting/linking/wrapping existing code before rewriting it**.

Expose provenance-bearing equivalents of the minimum shared concepts needed by the first Pratibimba surfaces:

```text
Bimba / EpiAddress / canonical M/M′ identity
M0–M5 family roots
six canonical M-domain Agent identities
QL relation / position / lens handles
Context Frames
VAK = CPF / CT / CP / CF / CFP / CS
MathemeHarmonicProfile or proven canonical successor
DAY/NOW / current tick / Kairos handles
coordinate + source/provenance handles
Nara protected identity/current-field/day-episode handles
Mahamaya/oracle/transcription handles only where already real
implementation status = implemented | partial | stub | research
```

Do not invent renderer-local versions of QL, VAK, the Matheme or harmonic profile.

Where QL-MEF already owns a canonical generalised operator, consume it explicitly with provenance. If a narrow missing operator blocks this tranche, implement only that dependency. Do not expand the tranche into completing the whole QL/MEF musical programme.

## S/S′ / TA-ONTA GUARD

Keep the current placement invariant:

```text
S4′ = full Ta-Onta agentic-inhabitation surface
  S4.0′ Khora
  S4.1′ Hen
  S4.2′ Pleroma
  S4.3′ Chronos
  S4.4′ Anima
  S4.5′ Aletheia
        ↓
S5′ Epii
```

Do not port Ta-Onta as a second generic harness. Bring in a carrier only when an actual agentic Epi act needs that constitutional law.

## ACCEPTANCE

This tranche is done when:

- current O:I desktop can discover/open an Epi/Pratibimba mode or contribution;
- Epi appears in the normal primary-canvas/agent-region application model rather than a new shell;
- real `epi-lib` / `portal-core` code is being called, not represented by decorative fixtures;
- one stable Epi address can be resolved with source/provenance;
- VAK/CF and current harmonic profile state can be read through the bridge;
- DAY/NOW/current tick can be obtained for a future Nara surface;
- Nara protected state primitives are available without leaking private payloads into generic O:I state;
- restart/deterministic fixtures preserve canonical identity and provenance;
- implemented/partial/stub/research states are truthful;
- no second generic session/harness/process/plugin ontology is introduced.

Produce a PR with exact verification evidence and leave a concise handoff for Prompt B.

---

# PROMPT B — M4′ NARA FIRST LIVED PRATIBIMBA VERTICAL

Work directly in the **actual live state after Prompt A**.

Primary repos:

- https://github.com/EpiLogos/O-I
- https://github.com/EpiLogos/Epi-Logos-C-Experiments
- https://github.com/EpiLogos/QL-MEF
- https://github.com/EpiLogos/Central where personal authored-ground binding is actually required
- AIKit/Actuation/Workcell only for their already-native host/agency/material contracts.

## SOURCE AUTHORITY

Read:

- QL-MEF #30/#33/#34/#35/#36;
- `EPI-PRATIBIMBA-BUILD-ORDER.md`;
- Epi `M'-SYSTEM-SPEC.md`;
- Epi `M4'/M4'-SPEC.md` and its current companion Nara specs;
- existing Nara renderer/editor/highlight/store code as migration/function evidence;
- Central's current authored-ground/proposal contracts;
- current O:I desktop host code and Prompt-A bridge.

## DESTINATION

Make the first **genuinely usable Pratibimba experience**:

> M4′ Nara in the O:I primary canvas, with the O:I situated agent able to co-refer to the same protected lived object.

The default experience is a journal/flow surface that **knows its current Epi/cosmic context without lecturing the user about the computational interior**.

## BUILD

Implement the smallest coherent daily surface:

```text
FLOW / JOURNAL
- click/write immediately
- protected persistence
- day-as-episode container

DAY/NOW
- current day/session/time handle
- current coordinate where relevant

RESONANCE / MATHEME CONTEXT
- current harmonic/profile state
- concise human-facing resonance/state
- provenance / Explain summonable

LEAN IDENTITY
- only safe current identity context required by the design
- no raw protected profile dump

HIGHLIGHT / SENDOFF
- selected text / source range
- typed governed Action
- current DAY/NOW + coordinate/profile handles

AGENT CO-REFERENCE
- Nara/Anima-capable situated agent receives the same stable episode/selection/context refs
- no DOM scraping
- no hidden generic Epi chat/session store
```

Use the O:I right agent/context region. Agent conversation can summon Nara inspectors/deeper surfaces into the primary canvas rather than permanently displaying technical panels.

## M4′ CONSUMES UPSTREAM INSTRUMENT HANDLES

M4′ may consume real available handles such as:

```text
M1 → tick / tone / harmonic role / QL position
M2 → resonance / element / decan / chakra / correspondential handles
M3 → codon / hexagram / Tarot / rotation / transcription handles
```

Do not recreate those laws inside React, the editor, or a Nara-local calculation service.

Missing deep capabilities should degrade explicitly. They do not justify blocking journal/day/agent co-reference if the minimum real context exists.

## PRIVACY / CENTRAL

Preserve:

```text
Authored source
!= Observation
!= Inference
!= Derived state
!= Proposal
!= Accepted durable mutation
```

Nara is protected-local by default. Generic O:I host availability is not permission to disclose journal/dream/private identity bodies.

Where Central is the human-owned durable ground, use its native proposal/acceptance discipline rather than creating a second profile database.

## ACCEPTANCE

A human can:

1. open O:I;
2. enter Epi/Pratibimba;
3. land in Nara/Personal daily view;
4. write into a real protected day episode;
5. see concise real DAY/NOW + harmonic/VAK/coordinate context;
6. select/highlight text;
7. invoke a governed agent action on that exact selection/context;
8. receive explanation/reflection with provenance;
9. restart and recover the correct day/episode/state;
10. inspect technical detail only when deliberately summoned.

Do not add full M1′/M2′/M3′ dashboards in this tranche.

Produce a PR with visual/interaction evidence, privacy tests, bridge integration tests and exact implementation status.

---

# PROMPT C — PERSONAL 4/5/0 + INTEGRATED COSMIC INSTRUMENT

Work from the **accepted live state after the Nara vertical**, not from the historical shell.

Primary repos:

- O:I
- Epi source
- QL-MEF
- Central / AIKit / Actuation / Workcell / Factory only where the native owner is actually crossed.

## SOURCE AUTHORITY

Read:

- Epi `M'-SYSTEM-SPEC.md`;
- `M0'/M0'-SPEC.md`;
- `M4'/M4'-SPEC.md`;
- `M5'/M5'-SPEC.md` while treating its older dedicated-Theia host topology as superseded by current O:I host architecture;
- integrated 4/5/0 recognition architecture;
- integrated 1/2/3 cosmic-engine architecture;
- QL-MEF #30/#31/#33/#34/#35/#36;
- current O:I desktop and accepted Nara implementation.

## DESTINATION A — PERSONAL 4/5/0

Turn the Nara daily vertical into one coherent lived-return composition:

```text
M4′ Nara
  lived writing / episode / question / current field
        ↓
M5′ Epii
  conversational pedagogy / explanation / review / proposal / return
        ↓
M0′ Anuttara/Bimba
  source-ground / coordinate / graph / canon orientation
        ↺
```

### UX

The primary surface remains calm and Nara-rooted.

Use the O:I situated-agent region for Epii/Nara/Anima conversation. When the user asks for depth, open/summon the relevant Bimba/source/review/workbench surface in the primary canvas.

Do not permanently embed M5′'s whole developer IDE or M0′'s full graph into the daily Nara page.

Generic coding-agent/session/run mechanics remain O:I/AIKit/Factory/Actuation. Epii supplies Epi-specific pedagogy, canon/source interpretation, review and return semantics.

## DESTINATION B — COSMIC M1′+M2′+M3′ AS ONE INSTRUMENT

Introduce the first explicit Cosmic face from **real shared kernel/profile outputs**:

```text
M1′ relational / mathematical-musical movement
 +
M2′ harmonic / correspondential / vibrational field
 +
M3′ symbolic / world-clock / transcription
        ↓
ONE CURRENT COSMIC / MATHEME INSTRUMENT
        ↓
feeds Nara context and can be opened directly
```

Do not build three unrelated dashboards.

The first Cosmic surface should favour:

- current clock/harmonic state;
- relation/movement;
- current resonance/correspondence;
- symbolic/transcription condition;
- selected Bimba coordinate/source orientation;
- graceful readiness disclosure for unavailable deep capabilities.

Reuse existing clock/Hopf/cosmic renderer logic where it remains sound, but consume the current shared primitive/profile contract rather than renderer-local recomputation.

## DEEP WORKSPACE ENTRY POINTS

Establish addressable routes/surface refs for M0′–M5′ but implement only the depth required by this tranche.

The deep subsystem programme follows **after** the Personal + Cosmic parent loop is working.

## ACCEPTANCE

- Personal feels like one Nara–Epii–Anuttara lived relation;
- Epii can explain/review the exact current Nara/Bimba object with provenance;
- Bimba/source context can be summoned without taking over the daily surface;
- one integrated Cosmic surface is driven by real M1/M2/M3 shared state;
- Nara consumes the same Cosmic handles rather than a second calculation path;
- O:I agent/session/harness remains the generic operating body;
- private Nara data stays protected;
- unavailable deep M′ faculties are explicit readiness states;
- M0′–M5′ deep workspaces are addressable for subsequent development without becoming six dashboard cards.

Produce a PR and handoff that identifies the **next experienced depth demand**, not simply the next numerical M coordinate.
