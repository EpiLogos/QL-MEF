# Cosmic / Personal integration inside K4–K10

Status: **authorised development plan, 2026-09-11; not an implementation receipt**.
Programme owner: [Kernel Rebuild Wayfinder](../KERNEL-REBUILD-WAYFINDER.md), #135.
Execution owners remain #128–#134. This is their integration contract, not another programme or coordinate registry.

Current aperture/clock refinement: [Paraśakti apertures and the Mahāmāyā clock centre](APERTURES-AND-CLOCK-CENTRE.md). M2-0 owns the complete eighteen-aperture architecture through the author's 0/1/2 and eight-pair branch structure. M3-0 is being articulated as its spatio-temporal reception field, realised at the existing M3-5-5/0 centre within M3-5. This is the scoped planning successor to older 16+1/M3-only aperture accounts; new coordinate promotion and coupled-clock implementation retain their own evidence obligations.

## 1. The product being built

The desktop has two parent surfaces of one feature reality:

- `epi.cosmic.123`: the M1/M2/M3 harmonic, resonant, symbolic and temporal instrument.
- `epi.personal.450`: Nara's lived personal field, with the Bimba map and the Epii agent operating in the same DAY/NOW World.

They are distinct from the six recursively deep M′ instruments. Opening depth preserves the parent event, selected subject and exact coordinate. The parent can stay open beside the deep instrument.

**M0′ is the Bimba map. M5′ is the Epii agent.** These are functional products, not names for an ambient backdrop and an abstract articulation layer. In ordinary work the map is available for locating, exploring and relating sources and coordinates; Epii is available for dialogue, investigation and action. Opening M0′ gives the full map workspace. Opening M5′ gives the Epii agent's full working surface over its source-designed capabilities, using the same canonical Agent and native session machinery. Neither opening creates a second map or another Epii.

M1′, M2′, M3′ and M4′ expose their full formal/topological, harmonic/correspondential, symbolic/form/clock and personal/phenomenological instruments respectively. The prime denotes the whole usable instrument, not merely a renderer or C++ class.

The owner's current implementation report establishes the planning baseline: the point-cloud engine is already cleanly integrated in the O:I desktop, including the seven planetary/chakral centres and dynamic glyph/field behaviour discussed in the source conversation. `EpiLogos/Point-Cloud-Demo` is the supplied engine source. Continue that implementation. Do not put rebuilding those capabilities, extracting another demo, selecting another shell, or replacing the working renderer on the critical path. The integration owner records the actual desktop/engine revisions and public hooks when binding it; an older demo snapshot does not negate the reported installed work.

## 2. Entry at K4, without restarting it

The owner is coordinating from K4. Live GitHub at this planning cut records #128 complete through merged PR #153, commit `5b24b95d17234ab5d23d84e658c0cc06434b41a3` (2026-09-11). Consume:

- [K4 census receipt](K4-M123-CENSUS.md);
- `fixtures/kernel/m-ledger-v1.json`;
- `fixtures/kernel/census/census-m{1,2,3}.json`;
- `fixtures/kernel/census/work-graph.json` and its reports;
- the accepted K2 source/registry identities and K3 ledger schema.

The census is implementation discovery, not whole-M operational or experiential acceptance. Existing K5/K6/K7 branches remain independent candidates until their own acceptance. This planning change neither advances nor reopens any stage and does not overwrite their branches.

K4's integration handoff is bounded: relate the already-discovered M1/M2/M3 producers to the consumers below and retain the full source paths for unseated cross-domain relations. Do not rerun the whole census solely to add a consumer requirement; do not enlarge K4 into M4 implementation, desktop acceptance or C++ construction. Later discoveries are recorded through the existing ledger refresh/discrepancy mechanism by their owning vertical. Generated census files and evidence are not hand-edited into a roadmap.

K10 uses the K4 method for its M0/M4/M5 census. That work can begin from the accepted common floor while K5–K7 finish; its own binding dependencies determine when each operation or renderer connection becomes executable.

## 3. One stack, with explicit implementation responsibilities

| Holding | Responsibility in this feature | Delivering stage |
|---|---|---|
| Bimba / Neo4j and source corpus | Coordinate meanings and relations, planetary/chakral/musical/colour correspondence paths, source and semantic graph work | K4 discoveries; K5–K7/K10 reconciliation |
| Native C | Stable coordinate/relation identities, canonical tables and structural operators, compiled correspondence facts where promoted | K5–K7/K10 |
| Rust | Typed domain operations, live provider resolution, current-event composition, personal identity/activity/trajectory, map and Epii domain Actions, evidence and Return | K5–K7/K10 |
| C++ | Continuous topology, resonant material, waves/audio, form/folding and subject-bound personal-field evolution | K8, consuming K5–K7 and K10 outputs |
| Existing point-cloud/GPU implementation | Retained granular material, seven centres, glyph formations, particle buffers, display and interaction, driven through its actual integration hooks | K8 adapter; K9/K10 consumers |
| O:I and native product owners | Canvas/surface placement, documents, selection, agent/session/capability access, temporal source ownership and material execution | Existing host and product seams; exercised by K9/K10 |

C/Rust and C++ consume the same exact identities and revisions. No universal requirement is imposed to call every language for every operation. The current C `libql-mef-c.a`, `ql/m_tree.h`, Rust `MRegistry`, music/MEF machinery, `TemplateureField` and `PhysicalPoleFormState` are inherited implementation, not requests for duplicate foundations. The current M4 matrix fixes `[w,x,y,z] = [Earth,Fire,Water,Air]`; historical alternate arrays do not reopen that decision.

### K8 boundary to decide and implement

K8 owns a small recorded runtime-binding decision against real vertical and desktop requirements: Rust/C++ in-process or supervised-process binding, public versioning and memory ownership, continuous-state transport, audio device output, and the existing WebView/GPU receiver. A versioned C ABI with opaque runtime handles is a candidate, not a newly ratified library requirement. Do not preselect a replacement graphics engine. Record the chosen path with an executable headless consumer and the actual renderer adapter.

The operational interface must carry coherent event/subject/coordinate refs, registry and contract revisions, generation, effective time, and source/derivation handles. Render snapshots can contain bounded coefficients, mode/phase/amplitude arrays, fields, transforms and material parameters. Larger binary fields or textures use a measured transport appropriate to the existing host. Rich graph queries, provider I/O and agent inference stay outside audio/render callbacks. Commands that change domain state return through the Rust owner and produce attributable receipts; view-local gestures remain presentation operations.

Domain changes, numerical integration/audio time and presentation frames have different cadences. Their times are related explicitly. One accepted generation feeds coherent sound, form and colour; a half-updated generation must not mix a new colour with an old musical or personal state. Muting gates audio output without replacing the harmonic state. Hiding or reopening a surface follows the runtime lifecycle and resumes the same subject; opening depth does not create another clock or restart the simulation.

## 4. M2 musical–colour–material producer: K6 / #130

This is a named consumer requirement of the full M2 vertical, not a replacement for its complete coordinate coverage.

Resolve the current relation across M1 harmonic substrate, M2-0 aperture architecture, M2-1 MEF/Vimarśā, M2-2 tattvic/elemental medium, M2-3 decanic condition, M2-4 symbolic-musical Power and M2-5 planetary–chakral synthesis. Preserve the exact Bimba relations linking planetary/chakral colours to the musical systems. The evidence must identify the source nodes and relation path, not merely a colour attached to a label.

The M2 output must provide jointly attributable:

- active MEF lens/local position, mode/tuning and relevant correspondence refs;
- harmonic drive, including the accepted `audio_octet` / `nodal_quartet` semantics and richer modal state as required;
- planetary/chakral identities and their resolved musical and colour relations;
- elemental carrier and named colour/material quantities;
- response controls such as damping, elasticity, particle affinity, phase, amplitude and envelope where supported by the selected embodiment;
- M3 handoff facts, keeping the existing discrete and distributed transforms separately identified;
- provider observation, source paths, derivation, revision and readiness.

The spec's material/modulation structs are useful recovered requirements; current K6 code and accepted contracts determine the concrete API. Resolve colour from the source correspondence path, then apply an explicit rendering conversion/palette policy. Do not manufacture an RGB lookup from a scalar resonance or imply that audible frequency uniquely determines optical colour. The correspondence is the explicit relation that makes the two outputs intelligible together.

Both parent surfaces and M2′ consume this producer. In the Cosmic instance it conditions the resonant/form body. In the Personal instance it is received through Nara's constitution and current state. The same source relation can therefore be inspected in the Bimba map, heard in the instrument and seen in the field.

Acceptance owned by K6: source-path and C/Rust relation/operation fixtures for selected full-chain examples, direct/conjugate and supported tuning cases, and serialisable state for K8. Continuous acoustic/visual equivalence is demonstrated by K8/K9/K10, not claimed merely from K6 descriptor parity. Unsupported correspondences remain identifiable and do not acquire made-up colours.

### 4.1 Aperture architecture and centre-field continuation

The owner assigns both additional apertures to M2-0-0/M2-0-1 and the sixteen static lenses to M2-0-2, organised through M2-0-2-0 through M2-0-2-7, each holding its reciprocal lens pair. M2 owns the aperture architecture and symbolic worlds; M3 receives their exact identities. The detailed naming proposal, native-index continuity and distinction from the twelve MEF lenses are recorded in [Apertures and clock centre](APERTURES-AND-CLOCK-CENTRE.md).

The 6° Fibonacci, 20° elemental-fibre and 22.5° form/void-ring grids are essential linked structural registers. Preserve their exact 60°/90°/180° pairwise closures, shared 180° closure and 9/8 relation alongside current phase offsets. They must reach the continuous producer with source/coordinate provenance rather than being recreated in a renderer.

M3-0 is the proposed common spatio-temporal field receiving M1 rotational/topological capacity and M2 spectral/aperture differentiation. Its realisation within M3-5 uses the existing centre M3-5-5/0, with a distinct field-ground relation rather than a new centre or coordinate alias. The inscription and lensing axes are independently identifiable and have explicit coupling/trajectory rules; a 720° clock plus a sixteen-entry selector alone does not prove that full twofold construction.

These requirements extend the accepted M2/M3 producers through the existing #135 programme; they do not retroactively broaden their acceptance receipts. K8 consumes the centre-field/coupled-phase contract, K9 exposes the assembled/exploded Cosmic and deep M2′/M3′ instrument, and K10 supplies inspection and authorised operation through the same Bimba map and Epii agent. The owner's flower/eye image means an inspectable composition whose parts can open out and return around one subject, not another particle-engine build.

## 5. Stage assignments and handoffs

| Existing stage | Integration work | Returned result |
|---|---|---|
| K4 / #128 | Preserve the returned census; use its workbooks to locate shared producers and unseated relation paths | Existing census plus this downstream consumer contract; no new completion gate |
| K5 / #129 | Complete M1; expose current carrier/topology, harmonic traversal, phase and event-bound operations for Cosmic and Personal consumers | Full M1 engine contract consumed by M2/K8/K9/K10 |
| K6 / #130 | Complete M2 including the musical–colour–material producer in §4 | Full M2 engine contract with jointly traceable sound/colour/material/correspondence output |
| K7 / #131 | Complete M3 and its clock/transcription parity; expose form/pose/transform/clock state and shared elemental handoff for personal reception | Full M3 engine contract, not an address64-only shortcut |
| K8 / #132 | Shared C++ runtime, domain engines, Personal-field capability and adapter to the already-built point cloud | Headless continuous engines, versioned handoff and working renderer binding |
| K9 / #133 | Compose Cosmic and deliver M1′/M2′/M3′ over the same engines; provide shared parent/deep open/operate/return machinery | Cosmic surface and deep instruments consuming the full state, ready to join Personal |
| K10 / #134 | Full M0/M4/M5 census/ports; Nara and Personal composition; Bimba map and Epii agent operations; remaining deep instruments and whole-M accounting | Personal parent plus M4′, functional M0′ map and M5′ Epii; joined two-parent/six-depth experience |

Dependency graph (work packages are inside existing tickets, not new stages):

```text
accepted K4 census
    ├── K5 M1 ──┐
    ├── K6 M2 ──┼── K8 shared continuous runtime ── K9 Cosmic + M1′–M3′
    └── K7 M3 ──┘                │                         │
                                 │                         │
K4 method / shared ledger ── K10 M0/M4/M5 producers         │
                                 │                         │
                                 └── K8 personal instance ─┤
                                                          │
                 K10 Personal + M4′ + Bimba map + Epii ────┘
                                joined parent/deep operation
```

K8's reusable personal-instance contract is designed with K10; its concrete production instance consumes K10's returned state. K10's producer work does not depend on K8, and K8's Cosmic work does not wait on K10. This is a producer/consumer join, not a circular launch gate. K9 does not wait for whole-K10 closure to deliver Cosmic; final joined acceptance records which participants are actually ready.

## 6. Personal field and remaining instruments: K10 / #134

K10 contains the following sequenced work inside the existing ticket.

### Recover and bind the whole field

Apply the existing census and ledger method to M0/M4/M5, retain the deep M4 matrix and all source branches, seat native C structures and align Rust operations. C-Experiments is read-only source/implementation provenance; new kernel/domain implementation lands in QL-MEF. The native host remains O:I.

### Publish Nara state and Actions

Implement the full M4-0 evidence field: birthdate, astrology, Jungian assessment, Gene Keys, Human Design and archetypal quintessence, with source-specific derivations and presence. Preserve M4-1 embodied condition, M4-2 oracle invocation of the integrated M1–M3 system, M4-3 transformation intelligence, M4-4 contextual/psychological/phenomenological and temporal work, and M4-5 integration.

Publish the personal state required by K8: stable subject and identity revision; `Q_identity`, `Q_transit`, `Q_activity`, `Q_composed`; bioquaternionic decomposition; elemental constitution and chakral receiving state; M1/M2/M3 refs; source/event/trajectory handles. Complete the actual derivation before marking each field operational. M3 form grammar, identity hash, live quaternion and bioquaternion retain their separate functions. Daily activity changes the lived composition and history; durable identity augmentation has its own explicit operation.

### Bind the existing field and DAY/NOW sources

K8 instantiates the personal continuous state from that producer and drives the already-built centres/glyphs/field. K10 exposes it through Personal and deep M4′. The parent is the everyday composed experience; M4′ gives the full identity, body, oracle, transformation, context, trajectory and recognitive instruments.

Consume current Central human Day, agent NOW, Flow/Dialogue, source/revision/history and reviewed-contribution operations at root and Project scope. A timestamp, a human Day and an agent NOW are linked but distinct. Several NOWs may span Days. Preserve source ownership; Graphiti or another temporal index is a derived retrieval surface, not another editable source authority. Retain both occurrence and receipt time, original generation and later interpretation. Day rollover must not reset identity or unfinished work. Replay and a new interpretation of a past episode are explicit different operations.

### Deliver M0′ — the Bimba map

Expose the actual map: search/open coordinates and source material, traverse typed relations, inspect a selected event's source/derivation path, move between overview and recursive depth, and retain selection when returning to either parent. Epii and the person resolve the same map objects. Existing graph/provider work is reused. Proposed map changes travel through the established reviewed-change operation; observing a field must not silently alter canon. M0′ acceptance requires usable map operations and actual graph content, not a decorative cityscape or a label saying ground.

### Deliver M5′ — the Epii agent

Bind the canonical Epii Agent to the same event/subject and the available domain Actions. It must be able to retrieve relevant Day/NOW/Flow and Bimba sources, inspect and explain M1–M4 state, invoke supported MEF/music/form/oracle/transformation operations, open a relevant deep instrument, investigate returned evidence, and return attributable findings or proposals through the native review/source path. Preserve the full source-designed gnostic, research/development, pedagogical and Logos capabilities rather than reducing Epii to an aura explainer.

M5′ is the full working interface to that same Epii agent. Ordinary Personal dialogue and deep M5′ share Agent identity and native session continuity. Actuation supplies agency/authority/Return, AIKit supplies models/capabilities/context/session access, Central supplies sources and temporal work, and Workcell supplies execution. Do not build a second agent loop, transcript runtime or universal tool router in QL-MEF or the renderer. Selection and visibility are not permissions; protected Nara sources are disclosed only within the actual authority of the action.

## 7. Joined feature acceptance

These are acceptance scenarios assigned to the stages above, not new prerequisite tickets.

1. **Two parents, six depths.** Switch Cosmic↔Personal and open/operate/return from every delivered M′ using the same event/subject/coordinate refs. The two parents do not become two independent computations. The Bimba map is M0′ and the agent is Epii/M5′ throughout.
2. **Sound, colour and field.** Choose a source-backed planetary/chakral/MEF relation; inspect its Bimba path, hear its musical expression and see the corresponding material/colour change from one generation. A supported mode/condition change updates the connected outputs. Verify mute/resume, stale input and renderer restart without semantic reset. K6 proves the derivation; K8 the continuous coupling; K9/K10 the experienced path.
3. **Personal activity and temporal continuity.** Use actual controlled Central Worlds at root and Project scope. Submit a permitted Flow/oracle/activity contribution, observe its M4.3 integration and field consequence, inspect the evidence, cross a Day boundary with an active NOW, and replay the original event. Protect the stable identity and human-authored bytes.
4. **Bimba-map use.** Navigate from a selected field centre or M3 form into its real coordinate/relations/sources and back. Inspect an Epii-proposed change and its accepted/rejected result through the owner path.
5. **Epii operates the instrument.** From one selected passage and event, Epii obtains authorised context, invokes an actual domain Action, observes the changed state and returns findings. The person and agent see the same result and provenance; this is an exercised tool/action path, not a prewritten chat demonstration. Include unavailable capability, refused authority and stale-revision paths.
6. **Continuous runtime.** Exercise headless and installed host paths for bounded state transfer, coherent generations, audio/render lifecycle, independent personal instances and unchanged event identity after deep-open. Retain the existing point-cloud behaviour while adding domain coupling.
7. **Aperture/centre composition.** Resolve all eighteen M2-owned apertures and eight reciprocal pairs; trace the 6°/20°/22.5° relations into M3-0 and the existing M3-5-5/0 centre. Exercise independent inscription/lensing changes and a specified coupled trajectory, preserving both phases, double-cover state and source bases. Assemble/explode/inspect/reassemble the same instrument without resetting its subject or treating view movement as a domain mutation. The detailed follow-on proof obligations are in [Apertures and clock centre](APERTURES-AND-CLOCK-CENTRE.md#7-current-implementation-and-deliberate-promotion).

Keep source, coordinate, relation, operational and experiential receipts separate. Planning or a passing shared protocol is not a completed instrument. Coverage closure records every known capability's disposition; operational feature closure additionally requires the promised operation to work.

## 8. Read with current authority

- O:I [`docs/positions/FOUNDING-POSITIONS.md`](https://github.com/EpiLogos/O-I/blob/main/docs/positions/FOUNDING-POSITIONS.md): authored ground, agency and Return.
- [Kernel Rebuild Wayfinder](../KERNEL-REBUILD-WAYFINDER.md), #135: sole K0–K10 programme owner.
- [Apertures and clock centre](APERTURES-AND-CLOCK-CENTRE.md): current M2-0 ownership/branch decision, essential three-grid links, M3-0 field-centre refinement and explicit twofold-clock follow-on work; scoped successor to older 16+1/M3-only aperture accounts.
- [K4 census](K4-M123-CENSUS.md): actual entry evidence and workbooks.
- [Integrated physical-pole object](../origami%20work/INTEGRATED-1-2-3-PHYSICAL-POLE-OBJECT.md) and current M1–M4 capability matrices: full domain requirements and canonical elemental continuity.
- QL-MEF #69/#73: same-event parent/deep, human/Agent access and existing O:I host relations. The present authorial identification of M0′/M5′ is explicit above; older ambient-language summaries are not functional substitutes.
- C-Experiments `Idea/Bimba/Seeds/M/M2'/M2-ARCHITECTURE.md`, `m2-prime-parashakti-cymatic-engine.md`, `M4'/M4-ARCHITECTURE.md`, `m4-prime-psychoid-cymatic-field-engine.md`, M4 identity/day/temporal material and M0′/M5′ source plans: source depth, not a new working repository or permission to revive retired host architecture.
- `Idea/Bimba/Map/datasets/parashakti-deep/**` and the current live Bimba map: exact planetary/chakral/musical/colour source relations.
- Central #150–#153 and their accepted successors: human Day, agent NOW, source operations, receiving and Return.
- `EpiLogos/Point-Cloud-Demo` and the owner's existing desktop integration: retained renderer implementation; exact installed revision to be recorded at binding.

The seven Context Frames remain settled. Parent `4/5/0` names the Personal composition, not an additional Context Frame. Consume the current accepted clock/aperture canon and vertical contracts rather than reinstating older counts from the source prose.
